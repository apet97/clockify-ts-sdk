# Plan 010: Add a nominative "why this vs other Clockify MCPs" positioning page + a visual quickstart (docs only)

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**:
> `git diff --stat 7c3a84c..HEAD -- mcp/README.md mcp/CHANGELOG.md docs/README.md NOTICE.md`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.
>
> **This is a DOCS-ONLY plan.** You create one new Markdown file plus a media
> folder, add one link line to `mcp/README.md`, and add one `mcp/CHANGELOG.md`
> entry. You change **no** source, no tool count, no JSON contract. You cannot
> produce images; you ship placeholders + an exact capture checklist for the
> maintainer.

## Status

- **Priority**: P2
- **Effort**: S
- **Risk**: LOW (docs only; no tool/prompt count change; no source/contract edits)
- **Depends on**: none (complements plan 005, which builds the `.mcpb` install bundle; this plan does not require 005 to be merged)
- **Category**: docs
- **Planned at**: commit `7c3a84c`, 2026-06-26

## Why this matters

This repo's MCP server has real, verifiable differentiation — workflow-first
tools, a dry-run → single-use `confirm_token` handshake on destructive writes,
an offline SSRF guard on webhook URLs, and typed structured receipts — but none
of it is **visible** to a human evaluating it. There are zero screenshots or a
demo GIF, and there is no page that explains, in plain language, how this server
compares to the alternatives (there is **no official Clockify MCP server**; the
field is community repos and general-purpose connector/aggregator platforms).
This plan adds (a) a nominative, trademark-safe comparison page and (b) a visual
quickstart (install → first call → log work) with placeholders and a precise
maintainer capture checklist. The payoff: a non-developer can see what the
server does and why it is safer before they install it.

## Current state

Facts you need, inlined (verified against the repo at the planned-at SHA):

- **No human-facing screenshots/GIF or positioning page exist.** `mcp/` contains
  only `README.md`, `CHANGELOG.md`, `LICENSE`, `src/`, `tests/`, etc. There is no
  `mcp/media/` directory and no `mcp/POSITIONING.md`.
- `mcp/README.md` — the public MCP doc. Structure (line numbers at planned-at SHA):
  - Lines 1–18: title `# @apet97/clockify-mcp-115`, intro paragraphs, and a
    "Product posture" paragraph ending with
    `[`docs/product-north-star.md`](../docs/product-north-star.md).`
  - Line 20: `## Install`.
  - **Generated regions you must NOT touch** (regenerated by `make readme-tables`
    from `docs/mcp-tools.json`):
    - between `## Workflow Tools` and `## Workflow Examples`, delimited by
      `<!-- BEGIN generated:mcp-workflow-tools -->` … `<!-- END generated:mcp-workflow-tools -->`
    - between `## Domain Tools` and `## Result Envelope`, delimited by
      `<!-- BEGIN generated:mcp-domain-tools -->` … `<!-- END generated:mcp-domain-tools -->`
    Do not edit between those heading pairs and do not touch the BEGIN/END markers.
  - `mcp/README.md` advertises **134 tools (21 workflow + 113 domain)** and a
    "Resources and Prompts" section listing `clockify://mcp/doctor` (no-network
    diagnostics) and the `clockify-workflow-plan` prompt.
- `NOTICE.md` (repo root) — the trademark/non-affiliation contract. Key lines you
  must honor verbatim in spirit:
  - "not affiliated with, endorsed by, sponsored by, or specifically approved by
    CAKE.com or Clockify."
  - "'Clockify' is a trademark of CAKE.com. The reference … is a nominative use to
    identify the upstream API … the `-115` / `115` suffixes are deliberate
    trademark distance."
  - "All other product names, trademarks … are the property of their respective
    owners. Use of these names does not imply any endorsement or relationship."
  So: nominative text only, **no Clockify logos/marks/wordmark styling**, no claim
  of endorsement, and any third-party product you name (aggregator platforms) must
  be neutral, factual, and hedged — no disparagement.
- **Verified differentiators to feature** (do not invent more; these are real):
  - Workflow-first tools plus full domain CRUD (the README's "Workflow Tools" /
    "Domain Tools" tables are the authoritative list — link to them, don't restate
    the count).
  - `dry_run` → single-use `confirm_token` handshake on high-risk workflow writes
    and destructive domain deletes
    (`mcp/src/orchestration/confirm-guard.ts`; documented in the README's
    "Workflow Examples" two-step write note).
  - Offline webhook-URL SSRF guard: rejects non-HTTPS, embedded credentials,
    private/loopback/link-local/CGNAT/metadata IPs
    (`mcp/src/orchestration/webhook-url.ts`).
  - Typed structured receipts: `ids`, `changed`, `warnings`, `next`, stable error
    codes, and `recovery` hints (README "Result Envelope" + "Stable error codes").
  - No-network diagnostics resource `clockify://mcp/doctor` and the
    `clockify_status` tool.
  - Credentials read from env (`CLOCKIFY_API_KEY`, `CLOCKIFY_WORKSPACE_ID`),
    server-side only, never logged (README "Configure").
  - Local stdio; a one-click `.mcpb` bundle path exists via plan 005.

### Gate reality you must rely on (verified, so you do not over-scope)

These determine exactly which gates can be affected. Trust them:

- **`make docs-drift`** (`scripts/check-docs-drift.mjs` +
  `docs/docs-drift-contract.json`) scans `AGENTS.md`, `CLAUDE.md`, `README.md`,
  the whole `docs/` tree, `wrapper/README.md`, `cli/README.md`, **`mcp/README.md`,
  `mcp/CHANGELOG.md`**, and a few wrapper config files. It does **NOT** scan
  arbitrary new files like `mcp/POSITIONING.md`. Forbidden patterns it enforces on
  the files it does scan: bare `clockify-sdk-ts` not followed by `-115`; `v0.2.0`;
  the old tool counts (`105 tools`, `17 workflow tools plus 88 domain tools`, …);
  and `TODO`/`TBD`. Your `mcp/README.md` link line and `mcp/CHANGELOG.md` entry
  MUST avoid all of these.
- **`make docs-counts`** (`scripts/check-docs-counts.mjs`) checks count
  consistency across generated JSON and a stale-prose denylist applied only to
  `README.md`, `wrapper/README.md`, `cli/README.md`, `mcp/README.md`, `CLAUDE.md`,
  `AGENTS.md`. The denylist includes things like `89 tools`, `123 tools`,
  `127 tools`. `134 tools` is **allowed**. A new `mcp/POSITIONING.md` is **not**
  scanned by this gate.
- **`make docs-quality`** (`scripts/check-docs-quality.mjs`) scans `README.md`,
  `wrapper/README.md`, `cli/README.md`, **`mcp/README.md`, `mcp/CHANGELOG.md`**,
  `docs/README.md`, and three operator docs for an unsupported-marketing-claim
  blacklist: `world-class`, `battle-tested`, `production-ready`, `best-in-class`,
  `just works`, `zero-config` (case-insensitive, hyphen/space variants), plus
  `TODO`/`TBD`. It also requires `mcp/README.md` to keep containing
  `@apet97/clockify-mcp-115`, `structuredContent`, `generated:mcp-workflow-tools`,
  `generated:mcp-domain-tools`. A new `mcp/POSITIONING.md` is **not** scanned.
- **`make user-docs`** (`scripts/check-user-docs.mjs`) requires `mcp/README.md` to
  keep containing the markers `Workflow Tools`, `Domain Tools`,
  `Resources and Prompts`, `Result Envelope`, `Telemetry`, `structuredContent`,
  `generated:mcp-workflow-tools`, … Do not remove any heading. A new mcp-local doc
  is not required by this gate.
- **`make docs-index-drift`** (`scripts/check-doc-index.mjs`) only validates that
  links in `docs/README.md` point to existing files, plus required links/wiring.
  It does **not** require new files to be linked, and it does not scan
  `mcp/README.md` at all. A new `mcp/POSITIONING.md` is invisible to it.
- **`make readme-tables-drift`** (`scripts/update-readme-tables.mjs --check`) only
  regenerates/diffs the `generated:` BEGIN/END regions in `cli/README.md` and
  `mcp/README.md`. Editing `mcp/README.md` **outside** those regions keeps it
  green; touching inside reds it.
- **`make changelog-drift`** requires the package changelog to be updated when you
  touch package files. Because this plan adds files under `mcp/` and edits
  `mcp/README.md`, you must add an `mcp/CHANGELOG.md` entry (Step 4).
- **No image-existence gate exists** (`ls scripts/check-*` has no link/image
  checker beyond `check-doc-index.mjs`, which only covers `docs/README.md`). Broken
  `![]()` image embeds would not red a gate, but they render as broken on GitHub —
  so use the placeholder convention in Step 2 (no live `![]()` to a missing file).

### Why mcp-local placement (decision, not optional)

Place the new doc at **`mcp/POSITIONING.md`** (mcp-local), NOT under `docs/`.
Rationale, from the gate reality above: a `docs/` file would be scanned by
`docs-drift`/`docs-counts`/`docs-quality` (more ways to red) and is expected by
convention to be registered in `docs/README.md` and wired into doc-index/contract
surfaces. An mcp-local doc is co-located with the server it describes, is not
scanned by those prose gates, and needs **no** `docs/README.md` registration. This
keeps `gateCascade` genuinely LOW. (If a future maintainer moves it under `docs/`,
they must register it in `docs/README.md` and follow the prose rules above — note
this in the file's footer, see Step 1.)

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Docs drift | `make docs-drift` | exit 0, "Documentation drift check passed." |
| Docs counts | `make docs-counts` | exit 0 |
| Docs quality | `make docs-quality` | exit 0 |
| User docs | `make user-docs` | exit 0 |
| Docs index drift | `make docs-index-drift` | exit 0, "docs index links are current" |
| README tables drift | `make readme-tables-drift` | exit 0, "README tables are current" |
| Changelog drift | `make changelog-drift` | exit 0 |
| Scope check | `git status --porcelain` | only the in-scope paths listed below |
| Optional final solo proof | `CLOCKIFY_API_KEY='' CLOCKIFY_WORKSPACE_ID='' make perfect-fast` | exit 0 (run SOLO; see caveat) |

> The focused gates above are the source of truth for this docs-only change.
> `make perfect-fast` is optional here and is **load-flaky** (its
> `performance-budgets` startup-time sub-check false-reds under CPU contention) —
> if you run it, run it solo and with creds blanked. Do not block on a
> performance-budget red; re-run solo to confirm.

## Scope

**In scope** (the only files you may create/modify):
- `mcp/POSITIONING.md` (create)
- `mcp/media/.gitkeep` (create — gives the placeholder image paths a home)
- `mcp/README.md` (modify — add ONE discovery link line near the top, OUTSIDE all
  generated regions)
- `mcp/CHANGELOG.md` (modify — one entry under `## [Unreleased]`)
- `plans/README.md` (modify — your status row, only if the index has a row for 010)

**Out of scope** (do NOT touch, even though they look related):
- `mcp/src/**` — no tool/logic/env change. This plan changes nothing the tool
  count, manifest, or contracts depend on.
- The `mcp/README.md` generated regions and their BEGIN/END markers.
- `docs/**` — no new doc under `docs/`, no `docs/README.md` index row for the new
  page (mcp-local by design), no contract JSON edits.
- `mcp/package.json` `"files"` (`["dist","README.md","LICENSE"]`) — leave it; the
  new doc and media are intentionally NOT packed, so `pack-snapshot` stays green
  with no regeneration.
- Any tool/prompt count surface (`docs/mcp-tools.json`,
  `docs/mcp-tool-manifest.json`, `docs/performance-budgets.json`,
  `docs/product-surface.*`, prose "134 tools" counts). The count does not change.
- Any Clockify logo, wordmark, brand color, or claim of affiliation/endorsement.

## Git workflow

- Branch: `advisor/010-nominative-positioning-and-visual-quickstart`
- Commit per logical unit; conventional commits, e.g.
  `docs(mcp): add nominative positioning page and visual quickstart`. Do NOT push
  or open a PR unless instructed.

## Steps

### Step 1: Create `mcp/POSITIONING.md`

Create the directory's media home first, then the page.

Create `mcp/media/.gitkeep` as an empty file (so the placeholder image paths have a
directory to land in and git tracks it).

Create `mcp/POSITIONING.md` with the content below. Use this exact structure. Keep
all language nominative and hedged (see the rules after the block). Do **not**
hardcode the total tool count — link to the README tables instead (this avoids
creating a stale-count liability that no gate would catch).

````markdown
# Clockify MCP server (community) — how it compares & a 2-minute quickstart

> Independent, community-built MCP server for the public **Clockify** HTTP API.
> **Not affiliated with, endorsed by, or sponsored by CAKE.com or Clockify.**
> "Clockify" is a trademark of CAKE.com, used here nominatively only to identify
> the upstream API this server integrates against. See [`NOTICE.md`](../NOTICE.md).
> Other product names below belong to their respective owners; mentioning them
> implies no endorsement or relationship.

## Why this server vs other Clockify MCP servers

There is no official Clockify MCP server today. The practical alternatives are
hand-built community MCP servers and general-purpose connector/aggregator
platforms (for example StackOne, Zapier, or Composio) that expose Clockify
through a generic connector. This server is purpose-built and workflow-first.
The table is a factual, nominative comparison to help you choose — capabilities
of third-party tools vary by version and plan, so their columns are hedged.

| Capability | This server (`@apet97/clockify-mcp-115`) | Typical community Clockify MCP | Connector / aggregator platform |
|---|---|---|---|
| Tool design | Workflow-first tools (log work, review day/week, invoice, request time off) **plus** full domain CRUD — see the [Workflow Tools](./README.md#workflow-tools) and [Domain Tools](./README.md#domain-tools) tables | Often a thin 1:1 mirror of the REST API | Generic API passthrough |
| Destructive-write safety | `dry_run` preview returns a single-use `confirm_token`; the write only runs when you replay it | Frequently writes directly, no preview | Varies by connector |
| Webhook URL safety | Offline SSRF guard rejects non-HTTPS URLs, embedded credentials, and private/loopback/link-local/CGNAT/metadata IPs before creating a subscription | Typically none | Varies |
| Result shape | Typed receipts: `ids`, `changed`, `warnings`, `next`, stable error codes, and `recovery` hints | Usually raw JSON passthrough | Usually raw JSON passthrough |
| Diagnostics | No-network `clockify://mcp/doctor` resource + `clockify_status` tool for a safe first check | Usually none | Platform dashboards |
| Credential handling | `CLOCKIFY_API_KEY` / `CLOCKIFY_WORKSPACE_ID` read from the environment, server-side only, never logged | Varies | Stored by the platform vendor |
| Install / run | Local stdio; optional one-click `.mcpb` bundle | Clone + build | Hosted sign-up / OAuth |
| Provenance | Open source, MIT, gate-checked; the Node sibling to a drift-gated Go reference server | Varies | Closed connectors |

Pick this server when you want a local, workflow-complete Clockify MCP with
preview-before-write safety and structured, recoverable receipts. Pick an
aggregator when you are already standardized on that platform and want one connector
surface across many tools.

## 2-minute visual quickstart

Three steps: install, make your first (read-only) call, then log work. The
screenshots below are placeholders until the maintainer captures them (see the
checklist at the end).

### 1. Install & configure

Install the server and give it your Clockify API key and workspace id. The
one-click `.mcpb` bundle prompts for both and stores the key in your OS keychain;
the manual MCP-client config is in the [README](./README.md#configure).

> Screenshot — `media/install-mcpb.png`: the install dialog showing the
> "Clockify API key" and "Clockify workspace ID" fields (values redacted).
<!-- maintainer: after capture, replace the blockquote above with: ![Install dialog with API key and workspace id fields](media/install-mcpb.png) -->

### 2. First call — confirm the connection (read-only)

Ask your assistant to run `clockify_status`. It reports your user, workspace, and
any running timer — a safe, read-only way to prove the connection works.

> Screenshot — `media/first-call-status.png`: the assistant calling
> `clockify_status` and the returned receipt (user/workspace redacted).
<!-- maintainer: after capture, replace the blockquote above with: ![clockify_status receipt](media/first-call-status.png) -->

### 3. Log work

Ask your assistant to log a finished entry, e.g. "log 9:00–10:15 on Website,
Implementation, description 'wire up auth'". The server resolves the names to
ids and returns a typed receipt with the created entry's `ids` and a `next`
suggestion.

> Screenshot — `media/log-work.png`: a `clockify_log_work` call and its success
> receipt (`ids` / `changed` / `next`).
<!-- maintainer: after capture, replace the blockquote above with: ![clockify_log_work success receipt](media/log-work.png) -->

> Full flow GIF — `media/quickstart.gif`: install → `clockify_status` →
> `clockify_log_work`, end to end.
<!-- maintainer: after capture, replace the blockquote above with: ![Install to first logged entry](media/quickstart.gif) -->

## Maintainer: screenshot & GIF capture checklist

Capture these into `mcp/media/` and then swap each placeholder blockquote above
for the `![...]()` line shown in its adjacent HTML comment.

- [ ] `install-mcpb.png` — the install/onboarding dialog with the "Clockify API
      key" and "Clockify workspace ID" fields visible.
- [ ] `first-call-status.png` — a `clockify_status` call and its receipt.
- [ ] `log-work.png` — a `clockify_log_work` call and its success receipt
      (`ids` / `changed` / `next`).
- [ ] `quickstart.gif` — the full install → status → log-work flow.

Capture rules:
- **Redact every secret and identifier**: blur or replace the API key, the
  24-character workspace id, real email addresses, and real client/project/task
  names. Use obviously-fake demo values (e.g. "Acme", "Website").
- Use a sandbox/test workspace, never a customer workspace.
- Target ~1400px wide for PNGs; keep the GIF under ~5 MB and a few seconds long.
- No Clockify logo, wordmark, or brand styling in the captures or the surrounding
  layout — nominative text only (see [`NOTICE.md`](../NOTICE.md)).

---

This page lives in `mcp/` on purpose (co-located with the server it describes and
intentionally not part of the npm tarball). If it is ever moved under `docs/`, it
must be registered in [`docs/README.md`](../docs/README.md) and follow the docs
prose rules (write the SDK as `clockify-sdk-ts-115`; no `TODO`/`TBD`; no stale
counts; no `world-class`/`production-ready`/`best-in-class`-style claims).
````

Rules to honor while writing the file:
- Nominative only: keep the non-affiliation disclaimer block at the top; no
  Clockify logo/wordmark/brand styling.
- Third-party names (StackOne/Zapier/Composio) appear once, neutrally, as examples
  of the aggregator category, with hedged wording ("varies", "often", "typically").
  Do not assert specific negative claims about a named product.
- Do NOT hardcode the total tool count; link to the README's `#workflow-tools` /
  `#domain-tools` anchors.
- No `TODO`/`TBD` anywhere (use "placeholder"/"pending capture" instead). No
  `world-class`/`battle-tested`/`production-ready`/`best-in-class`/`just works`/
  `zero-config`. (These gates do not scan this file, but keeping the file clean
  future-proofs it if it is ever moved under `docs/`.)
- Use the literal `![]()` image embeds only inside the HTML comments (so nothing
  renders as a broken image until the maintainer drops the real files).

**Verify**:
- `test -f mcp/POSITIONING.md && test -f mcp/media/.gitkeep && echo OK` → `OK`
- `grep -c "![" mcp/POSITIONING.md` shows the only `![` occurrences are inside
  `<!-- maintainer: ... -->` comments (run
  `grep -n "!\[" mcp/POSITIONING.md` and confirm every hit is on a comment line).

### Step 2: Add ONE discovery link to `mcp/README.md` (outside generated regions)

Insert a single pointer line between the "Product posture" paragraph and the
`## Install` heading — i.e. after the line ending
`[`docs/product-north-star.md`](../docs/product-north-star.md).` and before
`## Install`. Add this paragraph:

```markdown
New here? See [how this compares to other Clockify MCP servers and a 2-minute visual quickstart](./POSITIONING.md).
```

Do not place it anywhere between `## Workflow Tools`→`## Workflow Examples` or
`## Domain Tools`→`## Result Envelope`, and do not touch any `BEGIN/END
generated:` marker. Do not change or remove any existing heading (the
`user-docs`/`docs-quality` gates require the existing markers to remain).

The added line contains no forbidden token (no bare `clockify-sdk-ts`, no
`v0.2.0`, no `TODO`/`TBD`, no stale count, no marketing-blacklist word), so the
prose gates stay green.

**Verify**:
- `grep -n "POSITIONING.md" mcp/README.md` → one match, located before the
  `## Install` line (`grep -n "^## Install" mcp/README.md` shows a higher line
  number).
- `make readme-tables-drift` → exit 0, "README tables are current" (proves you did
  not disturb a generated region).

### Step 3: (deferred to maintainer) capture and embed the images

You cannot produce images. Leave the placeholders as-is. The capture checklist is
embedded in `mcp/POSITIONING.md` (Step 1) for the maintainer. No action here
beyond confirming the placeholders render as plain blockquotes (no broken
images).

**Verify**: `grep -n "Screenshot —" mcp/POSITIONING.md` → 3 matches;
`grep -n "Full flow GIF —" mcp/POSITIONING.md` → 1 match.

### Step 4: Record the change in `mcp/CHANGELOG.md`

Add an entry under the existing `## [Unreleased]` heading, matching the file's
bullet style. Suggested text (avoid forbidden tokens; do not cite a tool count):

```markdown
- docs: add `POSITIONING.md` — a nominative "how this compares to other Clockify
  MCP servers" page and a visual quickstart (install → first call → log work)
  with screenshot/GIF placeholders and a maintainer capture checklist. No tool,
  API, or count changes.
```

Do not remove existing changelog markers; `mcp/CHANGELOG.md` must keep containing
`@apet97/clockify-mcp-115` and `workflow tools` (a `docs-quality` requirement).

**Verify**: `make changelog-drift` → exit 0.

### Step 5: Run the focused docs gates

Run all of the docs gates that can be affected by editing `mcp/README.md` /
`mcp/CHANGELOG.md`:

```sh
make docs-drift docs-counts docs-quality user-docs docs-index-drift readme-tables-drift changelog-drift
```

**Verify**: every target exits 0. If any reds, read its message:
- a `docs-drift`/`docs-counts` failure naming `mcp/README.md` or
  `mcp/CHANGELOG.md` means your added text contains a forbidden token — fix the
  text, do not touch the contract.
- a `readme-tables-drift` failure means you edited inside a generated region —
  revert that part of the README edit.
- a `docs-quality`/`user-docs` "missing marker" failure means you removed an
  existing heading/marker — restore it.

## Test plan

No runtime logic changes → no unit tests. Verification is entirely via the
focused docs gates plus file-existence checks:

- `mcp/POSITIONING.md` and `mcp/media/.gitkeep` exist (Step 1).
- `mcp/README.md` links to `./POSITIONING.md` outside the generated regions, and
  `make readme-tables-drift` stays green (Step 2).
- `make docs-drift docs-counts docs-quality user-docs docs-index-drift
  readme-tables-drift changelog-drift` all exit 0 (Step 5).
- Optional, operator/reviewer, solo, creds blanked:
  `CLOCKIFY_API_KEY='' CLOCKIFY_WORKSPACE_ID='' make perfect-fast` exit 0.

## Done criteria

Machine-checkable. ALL must hold:

- [ ] `mcp/POSITIONING.md` exists, opens with the non-affiliation disclaimer
      block, contains the comparison table and the three-step quickstart, and
      contains the maintainer capture checklist.
- [ ] No raw `![]()` image embed points at a missing file: every `![` in
      `mcp/POSITIONING.md` is inside a `<!-- maintainer: ... -->` comment.
- [ ] `mcp/media/.gitkeep` exists.
- [ ] `mcp/README.md` contains exactly one `./POSITIONING.md` link, placed before
      `## Install`, and OUTSIDE every `generated:` region.
- [ ] `mcp/CHANGELOG.md` has a new `## [Unreleased]` entry for this change.
- [ ] `make docs-drift` exits 0.
- [ ] `make docs-counts` exits 0.
- [ ] `make docs-quality` exits 0.
- [ ] `make user-docs` exits 0.
- [ ] `make docs-index-drift` exits 0.
- [ ] `make readme-tables-drift` exits 0.
- [ ] `make changelog-drift` exits 0.
- [ ] `git status --porcelain` shows ONLY: `mcp/POSITIONING.md`,
      `mcp/media/.gitkeep`, `mcp/README.md`, `mcp/CHANGELOG.md` (and `plans/README.md`
      if you updated the index). `mcp/package.json` is unchanged.
- [ ] The tool/prompt count is unchanged (still 134) — you did not edit any count
      surface or generated region.
- [ ] `plans/README.md` status row for 010 updated (or the reviewer maintains the
      index).

## STOP conditions

Stop and report (do not improvise) if:

- The `mcp/README.md` structure at planned-at SHA does not match "Current state"
  (the "Product posture" paragraph or the `## Install` heading moved, or the
  generated-region markers differ) — the repo drifted; re-locate the safe insert
  point and report.
- `NOTICE.md`'s non-affiliation/trademark language changed since planned-at — the
  disclaimer block in Step 1 must match the current `NOTICE.md` stance; align it
  and report.
- Any focused docs gate reds and the only way you can see to fix it is to edit a
  file outside the in-scope list (a contract JSON, a generated region, or
  `mcp/package.json`) — that means the change is no longer docs-only; stop.
- A docs gate reds twice after a reasonable text fix.
- You find yourself wanting to add a Clockify logo/wordmark, claim affiliation or
  endorsement, or make a superlative/comparative claim that is not verifiable from
  the repo — stop; nominative + factual only.
- You consider putting the page under `docs/` instead — that changes the gate
  surface (docs-drift/docs-counts/docs-quality scan it, and it needs
  `docs/README.md` registration + contract wiring). Stay mcp-local per this plan;
  if there is a strong reason to move it, report rather than improvising the
  registration.

## Maintenance notes

For the human/agent who owns this after the change lands:

- **The images are intentionally deferred.** This plan ships placeholders and a
  capture checklist; a maintainer must capture the four assets into `mcp/media/`
  and swap each placeholder blockquote for the `![...]()` line in its adjacent
  HTML comment. Until then the page is text-complete and renders cleanly (no
  broken images).
- **Stale-count tradeoff of mcp-local placement.** `mcp/POSITIONING.md` is NOT
  scanned by `docs-counts`/`docs-drift`, so a count written there would go stale
  silently. That is why the page links to the README's generated tool tables
  instead of hardcoding a number — keep it that way.
- **Not packed.** `mcp/POSITIONING.md` and `mcp/media/**` are outside
  `mcp/package.json` `"files"`, so they are not in the npm tarball and
  `pack-snapshot` does not change. If a future maintainer adds them to `"files"`,
  regenerate `mcp/.packsnapshot` via `make pack-snapshot-check`.
- **If the page moves under `docs/`** it must be registered in `docs/README.md`
  (and likely added to the relevant `-contract.json` wiring), and it then becomes
  subject to the docs prose gates listed in "Current state".
- **Reviewer should scrutinize**: the comparison table is nominative and hedged
  (no endorsement claims, no disparagement of named third parties, no Clockify
  marks); the README link sits outside the generated regions; no count surface or
  tool count changed; the changelog entry avoids forbidden tokens.
- **Related**: plan 005 builds the `.mcpb` one-click bundle this page references in
  its "Install & configure" step. If 005 lands, the install screenshot should show
  the `.mcpb` dialog; if not, the manual-config path in the README still applies.

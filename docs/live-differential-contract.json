{
    "schemaVersion": 1,
    "purpose": "Differential gate between the live Clockify wire and the corrected OpenAPI response schemas. Every other gate in this repo checks internal consistency; this one checks that a generated type matches what the API actually returns. The failing direction is deliberate and asymmetric: a field present on the wire but absent from the schema is data the SDK silently drops, so it FAILS; a field in the schema but absent from the wire is usually an unpopulated optional, so it only warns. Schemas are compared rather than emitted .d.ts because the TypeScript types are generated from spec/corrected, so a field missing from the schema is missing from the type.",
    "wiring": {
        "makeTarget": "live-differential",
        "checker": "scripts/live/check-live-differential.mjs",
        "credentialed": true,
        "inAggregateGates": false,
        "reason": "Requires sandbox credentials and network, so it must stay out of contract-gates, perfect-fast, and perfect-full, all of which are offline and deterministic. Run it alongside perfect-live."
    },
    "sources": {
        "correctedOpenApi": "spec/corrected/clockify.corrected.openapi.yaml",
        "liveEvidenceManifest": "spec/evidence/live-evidence-manifest.json"
    },
    "scope": {
        "statuses": ["live-success"],
        "proofKinds": ["read-only"],
        "methods": ["get"],
        "excluded": [
            "write operations (this gate never creates, mutates, or deletes)",
            "operations whose path ids cannot be resolved without inventing one",
            "operations with no JSON 2xx response schema",
            "empty collections, which prove nothing about field shape"
        ]
    },
    "minimumCheckedOperations": 15,
    "minimumCheckedOperationsRationale": "The first run compared 18 operations. The floor sits at 15 rather than 18 on purpose: the comparable set legitimately shrinks when a collection is empty on a quiet sandbox or a request is rate-limited, and pinning the floor to one run's exact count would turn ordinary variance into a red gate. Its job is to catch the surface COLLAPSING -- which would make this gate silently vacuous -- not to assert an exact count.",
    "knownDrift": [],
    "requestDelayMs": 120,
    "receiptPath": "docs/live-differential-receipt.json",
    "redaction": {
        "valuesCaptured": false,
        "note": "The receipt records operation ids and FIELD NAMES only. No response values, no ids, no credentials."
    },
    "knownDefectsThisGateWouldHaveCaught": [
        "Client.ccEmails and Client.currencyId dropped by a thin shadowing schema",
        "Webhook.deliveryEnabled and Webhook.planEnabled dropped the same way",
        "SharedReport declared public/url while the wire returns isPublic/link, which made the CLI/MCP --public flag a silent no-op",
        "ExpenseHydratedDtoV1 was a bare allOf of ExpenseDtoV1, so the workspace-expenses list dropped 16 paths (the whole category and project objects, task, and fileName) while declaring three flat ids the list wire never returns. Found by this gate's first run 2026-07-28, fixed 2026-07-29; the same first-writer-wins shadowing as the Client and Webhook defects above.",
        "BalanceDtoV1 omitted negativeBalanceUsed, so callers could not see whether a negative balance had been drawn down. Found by this gate's first run 2026-07-28, fixed 2026-07-29."
    ]
}

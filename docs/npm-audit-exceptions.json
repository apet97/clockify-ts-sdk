{
  "schemaVersion": 1,
  "purpose": "Governed, expiring exceptions for npm audit advisories across production and development dependencies that have no upstream fix. Every entry needs a reachable-code assessment, upstream tracking, and an expiry; scripts/check-npm-audit.mjs fails closed on unexcepted, expired, severity-drifted, and stale entries.",
  "auditCommand": "npm audit --json",
  "exceptions": []
}

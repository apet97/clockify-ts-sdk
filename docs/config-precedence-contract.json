{
  "schemaVersion": 1,
  "purpose": "Configuration precedence contract for SDK options/env fallback, CLI flags/env/rc files, local MCP stdio env startup, remote MCP OAuth/PostgreSQL credential routing, base URL override safety, and missing-config recovery wording.",
  "policyDocument": {
    "path": "docs/config-precedence-policy.md",
    "contains": [
      "Surface precedence",
      "Base URL override rule",
      "Missing configuration errors",
      "Change rules",
      "Proof gates",
      "CLOCKIFY_API_KEY wins over CLOCKIFY_ADDON_TOKEN",
      "Command-line flags win over env vars",
      "Process env only",
      "MCP remote HTTP identity/credential/routing",
      "There is no ambient or shared-key fallback",
      "Authenticated-host equality",
      "Every authenticated path shares one host allowlist",
      "wrapper/tests/authenticated-host-equality.test.ts"
    ],
    "forbiddenMarkers": [
      "TODO",
      "TBD",
      "rc file override a flag",
      "normal production setting"
    ]
  },
  "requiredTargets": [
    "config-precedence",
    "env-contract",
    "user-docs",
    "cli-contract",
    "mcp-contract"
  ],
  "requiredDocs": [
    "docs/config-precedence-policy.md",
    "docs/env-contract.json",
    "docs/install-personas.md",
    "wrapper/README.md",
    "cli/README.md",
    "mcp/README.md"
  ],
  "surfaces": [
    {
      "id": "sdk-auth",
      "evidence": "wrapper/create-client.ts",
      "markers": [
        "CLOCKIFY_API_KEY is preferred over",
        "pass only one of `apiKey` or `addonToken`",
        "must provide exactly one of `apiKey` or `addonToken`"
      ]
    },
    {
      "id": "cli-precedence",
      "evidence": "cli/src/config.ts",
      "markers": [
        "lowest → highest precedence",
        "CLOCKIFY_API_KEY / CLOCKIFY_WORKSPACE_ID / CLOCKIFY_BASE_URL env vars",
        "--workspace / --base-url command-line flags",
        "firstPresent(env.CLOCKIFY_API_KEY)",
        "legacy rc-file secret detected",
        "Set CLOCKIFY_API_KEY",
        "Provide --workspace, set CLOCKIFY_WORKSPACE_ID",
        "failed to read Clockify rc file"
      ]
    },
    {
      "id": "mcp-stdio-env-only",
      "evidence": "mcp/src/client.ts",
      "markers": [
        "CLOCKIFY_API_KEY is not set",
        "CLOCKIFY_WORKSPACE_ID is not set",
        "one-user server is pinned to a single workspace",
        "CLOCKIFY_BASE_URL"
      ]
    },
    {
      "id": "mcp-remote-env-boundary",
      "evidence": "mcp/src/http-main.ts",
      "markers": [
        "rejectLocalCredentialEnvironment(env)",
        "CLOCKIFY_MCP_OAUTH_ISSUER",
        "createPostgresContextResolver"
      ]
    },
    {
      "id": "mcp-remote-credential-routing",
      "evidence": "mcp/src/remote/context.ts",
      "markers": [
        "credentials.load(principal)",
        "buildRoutingOptions(credential.region, credential.subdomain)",
        "effectiveScopes(principal.tokenScopes, credential.maxGrant)"
      ]
    },
    {
      "id": "authenticated-host-equality",
      "evidence": "wrapper/internal/authenticated-boundary-fetch.ts",
      "markers": [
        "Authenticated-host equality",
        "CLOCKIFY_PROD_HOSTS",
        "fails closed on drift"
      ]
    }
  ],
  "supportingEvidence": [
    {
      "path": "docs/env-contract.json",
      "contains": [
        "CLOCKIFY_API_KEY",
        "CLOCKIFY_ADDON_TOKEN",
        "CLOCKIFY_BASE_URL",
        "mustMentionSafety"
      ]
    },
    {
      "path": "cli/README.md",
      "contains": [
        "Configuration",
        "Command-line flags",
        "Environment variables",
        "CLOCKIFY_HOME"
      ]
    },
    {
      "path": "mcp/README.md",
      "contains": [
        "process environment",
        "CLOCKIFY_API_KEY",
        "CLOCKIFY_WORKSPACE_ID",
        "CLOCKIFY_BASE_URL"
      ]
    },
    {
      "path": "wrapper/README.md",
      "contains": [
        "CLOCKIFY_API_KEY",
        "CLOCKIFY_ADDON_TOKEN",
        "priority"
      ]
    },
    {
      "path": "wrapper/tests/authenticated-host-equality.test.ts",
      "contains": [
        "authenticated-host equality",
        "CLOCKIFY_API_HOSTS",
        "emits per-operation hosts only from the shared allowlist",
        "rejects near-miss hosts at the constructor and fetch boundaries alike"
      ]
    }
  ],
  "wiring": {
    "makeTarget": "config-precedence",
    "checker": "scripts/check-config-precedence.mjs",
    "docsIndex": [
      "config-precedence-policy.md",
      "config-precedence-contract.json"
    ],
    "qualityGate": "make config-precedence",
    "inventoryId": "config-precedence",
    "auditId": "config-precedence"
  }
}

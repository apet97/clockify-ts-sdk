All done. The findings file is complete and credential-clean.

## Completed

Audited all 11 ledger entries in my group (ROUTING + HOSTS + AUTH + ERRORS + IDEMPOTENCY + SURFACE leftovers + APPROVALS/USERS/GROUPS) plus the cross-cutting security-scheme/servers/`x-clockify-host` questions, using spec reads (committed official, corrected, live-fetched official), code inspection (wrapper + generator + GOCLMCP script), and **25+ live probes** against the sacrificial sandbox (`DSAUDIT_`-prefixed, `Leftovers: 0` verified).

Headline results:
- **`pto.api.clockify.me` is ALIVE** — serves the identical API gateway with identical credentials (200 on /user and /workspaces with valid key; 401 code 1000 without). The matrix's "dead/speculative" evidence claim is refuted; the routing decision (no op routes there) stands.
- **Idempotency re-confirmed unsupported**: same Idempotency-Key + different names → both 201, distinct ids, no echo headers.
- **Approvals filter confirmed**: PENDING/WITHDRAWN_APPROVAL → 200; REJECTED and bogus → 400 with the exact 3-value allowed set in the message.
- **Audit-log closed enum confirmed**: invented action → 400; CREATE_PROJECT → 200; corrected enum matches live official spec **37/37 byte-identical**.
- **removeMember route is BOUND** (400 code 501 "User doesn't belong to Workspace" on fake id) despite official's "not functional" description.
- **All 3 deliberately-unpromoted routes confirmed real** (structured 400s, not phantom).
- **Regex classifier**: 3 new live instances of "doesn't belong to Workspace" (Category/Project/User), all id-not-found shape, zero counterexamples — ledger's "leave unchanged" decision reaffirmed.
- **user-groups.get void method no longer exists** (quarantined GET-by-id; list-scan compensation intact).

## Files Changed
- `/tmp/clockify-openapi-audit/findings/routing-auth-errors.md` — the findings report (240 lines: summary table, 11 per-entry reports, cross-cutting section, 9 unledgered differences, security note, cleanup attestation)
- `/tmp/clockify-openapi-audit/probe-routing-auth.sh`, `probe-routing-auth2.sh` — probe drivers
- `/tmp/clockify-openapi-audit/results/*.{body,status,hdr}` — raw probe captures (credential-free)
- **Zero repository files touched** (repo read-only, honored)

## Notes
- **Security**: `/tmp/clockify-openapi-audit/findings/probes/.curl.cfg` (created 21:04 by a parallel probe helper, mode 600) contains the base64 API key in the shared aggregation area — recommend the parent delete it after parallel groups finish. My artifacts verified clean (grep scan).
- Repo has pre-existing dirty state (staged `Makefile`, `docs/*`, `scripts/*`; untracked `.ai-audit/`, `.pi-subagents/`) — present before this session; this audit staged nothing.
- Unledgered differences (9 total) include: pto host alive, removeMember bound, live official spec (175 ops) ahead of committed snapshot (158), audit-log `authors` object-shape probe trap, createExpense multipart-only (415 on JSON), expense minor-units, stale MCP comment, filter-vs-entity-state enums, official scheme-set drift.
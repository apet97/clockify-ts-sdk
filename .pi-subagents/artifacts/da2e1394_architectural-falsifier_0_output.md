I have completed my independent inspection and the challenge phase. Compiling the report now.

---

```json
{
  "taskId": "architectural-falsifier-clockify-ts-sdk",
  "repositoryHead": "931ac0114fe98211f96f82b0a3e76b145b261150",
  "workingTreeState": "?? .ai-audit/  ?? .pi-subagents/",
  "independentObservations": [
    "Makefile defines 179 targets, not ~40 (grep -c '^[a-zA-Z0-9_.-]*:' Makefile = 179; .PHONY at Makefile:1-4)",
    "All three packages ship only [dist, README.md, LICENSE] (wrapper/package.json, cli/package.json, mcp/package.json files fields); docs/, scripts/, wrapper/internal are NOT in any published tarball",
    "wrapper/src is gitignored (wrapper/.gitignore 'src/'); git ls-files wrapper/src = 0; wrapper/src is regenerated by scripts/generate-sdk-from-openapi.mjs via make sdk-codegen",
    "wrapper root has 30 tracked module .ts files + vitest.config.ts (git ls-files wrapper/ | grep '^wrapper/[a-z-]+\\.ts$' = 30), matching AGENTS.md:397 'currently 30, excluding vitest.config.ts'",
    "docs/live-evidence-currentness.json inputHashes pins SHA-256 of the Makefile (verified entry 'Makefile': '70985c4c...') - every Makefile edit forces a live-evidence campaign re-run",
    "docs/test-wiring-contract.json explicitly quarantines scripts/check-aggregate-wiring.test.mjs and scripts/check-mcp-write-safety.test.mjs with documented reasons (in-place tracked-file rewrites)",
    "cli/src/error-codes.ts and mcp/src/error-codes.ts are byte-identical (diff empty, both 298 lines) and both carry 'Generated by scripts/generate-error-docs.mjs' headers; generator emits wrapper/error-codes.ts too (scripts/generate-error-docs.mjs:11-16)",
    "docs/agent-handoff-contract.json pins exact mustContain strings in AGENTS.md and CLAUDE.md; make agent-handoff is in perfect-fast (Makefile:147) and governance-contracts (Makefile:159)",
    ".release-please-manifest.json and release-please-config.json are read by scripts/check-version-consistency.mjs:145-177 (release-contracts gate); only the workflow file was retired (git c5e7b97)",
    "docs/operation-parity.json (3,223 lines) drives mcp/tests/operation-parity.behavioral.test.ts:14 - a runtime in-memory MCP server test",
    "docs/service-routing-matrix.json:6-8 self-documents 'No runtime code imports this file'; docs/service-routing-matrix-policy.md:8-12 repeats it; wrapper/tests/routing-matrix-equality.test.ts is the deliberate bridge",
    "cli/src/client.ts and mcp/src/client.ts both hand-write REGIONAL_PREFIXES/KNOWN_REGIONS/buildRoutingOptions (~40 lines each) with different UX error strings (CLI --region vs MCP CLOCKIFY_REGION)",
    "output/scratch-*.mjs (12 tracked files) have zero references in Makefile, CI, scripts, or docs (grep verified); last touched by commit a8d61d0",
    "wrapper/package.json exports has 28 entries (root + 27 subpaths); docs/one-point-zero-classification.json notes document the measured keep-all-28 decision (health 1.2kB / otel-hooks 4.0kB / diagnostics 8.2kB; CJS cannot tree-shake)",
    "git 11e1ce2 documents four gates that had stopped guarding (openapi-lint stale at 163 ops while surface is 168, enterprise-audit stale marker, aggregate-gates missing sequence entry) - un-wired gates rot silently",
    "AGENTS.md:22-64 documents the SSRF-guard regression: wrapper/webhook-url.ts lost ~9.5 mutation points and gained 14 mutants with zero test coverage between 2026-07-31 and 2026-08-05 while every other gate stayed green",
    "No 1.0.1 git tags exist locally (38 tags; newest per package: wrapper-v1.0.0, cli-v1.0.0, mcp-v1.0.0); package.jsons and .release-please-manifest.json say 1.0.1; 'published 1.0.1' is UNVERIFIED offline",
    "docs/contract-inventory.json has 93 wiring entries + retiredGates with reasons; docs/gate-tier-inventory.json has 87 decision rows + 4 aggregate rows",
    "git describe = mcp-v1.0.0-14-g931ac01; 16 commits between wrapper-v1.0.0 and HEAD",
    "../GOCLMCP exists locally (Makefile + scripts/gen-clockify-openapi present); Makefile:249-252 goclmcp-drift and Makefile:255-271 spec-sync-drift (byte-faithful SHA-256 comparison) consume it"
  ],
  "challenges": [
    {
      "candidateId": "H1",
      "verdict": "rejected",
      "challengeSummary": "Of 261 tracked .mjs files under scripts/, 197 are invoked by literal path in Makefile/.github/package.json; the rest are libraries imported by other scripts (scripts/plan.mjs imports 10 topic modules) or tests wired via node --test; the only 'unwired' tests are explicitly quarantined in docs/test-wiring-contract.json, and the meta-gate scripts/check-test-wiring.mjs fails on genuinely unwired tests. No dead cluster exists in scripts/.",
      "contradictoryEvidence": [
        "docs/test-wiring-contract.json quarantine entries for check-aggregate-wiring.test.mjs and check-mcp-write-safety.test.mjs (documented disposition, not deadness)",
        "output/scratch-*.mjs (12 files) - zero consumers, but they live in output/, not scripts/",
        "git 82a8c34 'feat(gates): fail when a scripts/ test is wired to nothing' + docs/test-wiring-contract.json purpose (24 of 51 test files were once unwired; the meta-gate exists precisely to prevent silent dead tests)"
      ],
      "supportingEvidence": [
        "scripts/plan.mjs:13-22 imports acceptance-plan, change-impact-plan, examples-plan, maintenance-plan, onboarding-plan, workflow-plan, performance-calibration-plan, release-decision-plan, contract-inventory-report, risk-status-report",
        "budget-fingerprint.mjs imported by scripts/check-performance-budgets.mjs:6; build-replay-fixtures.mjs invoked by scripts/probe-and-stamp.mjs:69; readiness-test-fixtures.mjs imported by check-release-readiness.mjs:8; mcpb-artifacts.mjs imported by build-mcpb.mjs:47",
        "git log --diff-filter=D shows the repo actively deletes dead scripts (find-doc-integrity.mjs, sync-sdk.sh, deno-smoke.ts, release-please.yml) - pruning practice exists"
      ],
      "hiddenConsumers": [
        "scripts/plan.mjs topic modules (10 files) - consumed only via 'node scripts/plan.mjs <topic>' (AGENTS.md:368-373)",
        "Makefile:171-179 aggregate-gates target - runs scripts/lib/*.test.mjs + check-aggregate-gates.external.test.mjs + check-test-wiring.mjs",
        "docs/test-wiring-contract.json unwiredTests list - consumed by check-test-wiring.mjs (allowlist)"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "docs/test-wiring-contract.json expectedTestFileCount: 72 - deleting a wired test file reds the meta-gate",
        "Multiple checkers assert on literal 'node --test scripts/<name>.test.mjs' substrings in the Makefile (docs/test-wiring-contract.json purpose text)"
      ],
      "intentionalDuplication": [
        "The two quarantined tests duplicate the wiring checks of their siblings but are deliberately unwired because they rewrite tracked files in place (docs/test-wiring-contract.json reasons)"
      ],
      "smallestValidRemediation": "Leave scripts/ alone; the only truly unreferenced artifacts are output/scratch-*.mjs, which could be archived to git history after confirming zero risk-register references (none found).",
      "confidence": "high"
    },
    {
      "candidateId": "H2",
      "verdict": "rejected",
      "challengeSummary": "Every docs/*.json except live-differential-receipt.json has at least one consumer in scripts/, wrapper/, cli/, mcp/, .github/, or the Makefile; live-differential-receipt.json is a gate-written receipt referenced by docs/live-differential-contract.json:30 and docs/README.md:158. The JSONs feed runtime tests (operation-parity), gate pointers (docs-counts), generators (one-point-zero inventory), and drift checks; deletion cascades through docs-index-drift (139 required links), contract-inventory (93 entries), and gate-tier-inventory (87 rows).",
      "contradictoryEvidence": [
        "mcp/tests/operation-parity.behavioral.test.ts:14 reads docs/operation-parity.json and boots the real MCP server in-memory - deleting the JSON breaks a runtime test",
        "docs/docs-counts-contract.json authoritativeCounts point into docs/openapi-operations.json, docs/operation-parity.json, docs/mcp-tools.json",
        "scripts/generate-one-point-zero-inventory.mjs:11-19,122 reads docs/one-point-zero-classification.json as a required input (test at generate-one-point-zero-inventory.test.mjs:35-36)",
        "scripts/check-doc-index.mjs + docs/docs-index-contract.json expectedRequiredLinkCount: 139 - docs/README.md must link every indexed file",
        "docs/contract-inventory.json entries (93) map each gate to its contract JSON; docs/risk-register.json has 19 consumers"
      ],
      "supportingEvidence": [
        "Consumer counts measured: risk-register 19, openapi-operations 20, mcp-tool-manifest 15, product-surface 13, sdk-public-api 11, error-codes 17, consumer-cast-budget-contract 2 (both the checker and its test), service-routing-matrix 9",
        "docs/live-differential-receipt.json is the single 0-consumer JSON but is a receipt output of check-live-differential.mjs:155-174 and referenced by its contract"
      ],
      "hiddenConsumers": [
        "docs/one-point-zero-classification.json -> scripts/generate-one-point-zero-inventory.mjs (--check mode gated via its test)",
        "docs/operation-evidence-map.json + operation-dispositions.json -> scripts/generate-operation-parity.mjs",
        "docs/consumer-cast-budget-contract.json -> scripts/check-consumer-cast-budget.mjs:13 and wrapper/tsconfig.json (publicNoAnyProof compiler gate)"
      ],
      "generatedOwnership": "docs/one-point-zero-surface-inventory.json/.md, docs/gate-tier-inventory.json/.md, docs/product-surface.json, docs/error-codes.md, docs/openapi-operations.json, docs/operation-parity.json, docs/mcp-tool-manifest.json are generated outputs with --check drift gates",
      "compatibilityConstraints": [
        "docs-index-drift (Makefile docs-index-drift target; 139 required links)",
        "docs-counts gate derives headline counts from three docs JSONs",
        "contract-inventory + gate-tier-inventory + enterprise-hardening-audit registers every docs contract (five-anchor wiring per scripts/check-agent-tasks.mjs:1-15)"
      ],
      "intentionalDuplication": [
        "Contract JSON + policy MD + checker script trio is the deliberate per-gate pattern (93 entries in contract-inventory.json); JSON is the machine-checkable half, MD is the human half"
      ],
      "smallestValidRemediation": "Leave alone; no docs JSON is deletable without coordinated regeneration of at least four inventories plus the docs index.",
      "confidence": "high"
    },
    {
      "candidateId": "H3",
      "verdict": "rejected",
      "challengeSummary": "docs/ is not in the published tarball (wrapper/package.json files: [dist, README.md, LICENSE]), so runtime code cannot import docs/service-routing-matrix.json in the published package; the JSON is an approval artifact with provenance (approvedBy/approvedDate/sourceRevision at docs/service-routing-matrix.json:8-10) and non-runtime rows (pto url:null + unsupportedReason); the policy explicitly forbids runtime imports (docs/service-routing-matrix-policy.md:8-12); the equality test is the deliberate bridge and checks both directions (wrapper/tests/routing-matrix-equality.test.ts:88-119).",
      "contradictoryEvidence": [
        "docs/service-routing-matrix.json:6-8: 'No runtime code imports this file -- wrapper/internal/routing.ts keeps an equality-pinned hand-written copy...'",
        "docs/service-routing-matrix-policy.md:8-12: '**No runtime code imports this file.** It stays evidence tooling'",
        "wrapper/package.json files field excludes docs/; tsc CJS emit would need the JSON inside the package",
        "check-no-generated-edits.mjs pins only spec/corrected/, output/ts-sdk/, wrapper/src/ as generated paths - routing.ts becoming generated would require rewriting that gate and the codegen chain",
        "Matrix rows carry urlTemplate + liveConfirmationRequired + sourcePointer semantics that are not 1:1 with the runtime Record<ClockifyService, string> map (audit/pto are null per profile)"
      ],
      "supportingEvidence": [
        "wrapper/tests/routing-matrix-equality.test.ts:88-119 asserts every routable matrix service is present in ClockifyService (direction matters - a revived service is invisible to the union otherwise)",
        "The same hand-copy + equality-test pattern is used for CLOCKIFY_PROD_HOSTS (wrapper/tests/authenticated-host-equality.test.ts, referenced at routing-matrix-equality.test.ts:15-17)"
      ],
      "hiddenConsumers": [
        "scripts/service-routing-matrix.mjs (345 lines) validates the JSON against the corrected spec at make service-routing-matrix (Makefile:389-391, part of product-contracts)",
        "wrapper/tests/routing-matrix-equality.test.ts reads the JSON via fs.readFileSync at test time only"
      ],
      "generatedOwnership": "docs/service-routing-matrix.json is derived by scripts/service-routing-matrix.mjs from spec/corrected/clockify.corrected.openapi.yaml; wrapper/internal/routing.ts is hand-written by design",
      "compatibilityConstraints": [
        "Published tarball cannot import the JSON (files field)",
        "Dual ESM/CJS build would need resolveJsonModule + the JSON inside the package"
      ],
      "intentionalDuplication": [
        "routing.ts rows vs matrix JSON - deliberately duplicated with an equality test, per policy; the JSON stays evidence tooling, the TS stays runtime"
      ],
      "smallestValidRemediation": "Leave alone; single-sourcing would either ship docs/ in the tarball, add build-time codegen of a hand-written module, or invert the approval-evidence direction - all worse.",
      "confidence": "high"
    },
    {
      "candidateId": "H4",
      "verdict": "rejected",
      "challengeSummary": "The premise (~40 gates) is wrong - there are 179 targets but only ~6 aggregates; the Makefile deliberately avoids duplicated leaf lists (Makefile:165-168 comment: verify.mjs is 'sole authority'; 'no duplicated literal leaf list is maintained here'). The gate graph is itself a contract: aggregate-gates checks exact-once execution, gate-tier-inventory (87 rows) and contract-inventory (93 entries) register every target, quality-gates.md documents tiers, and the Makefile is a SHA-256-pinned input of live-evidence currentness - merging/removing gates reds multiple gates at once and forces a campaign re-run. The repo's own history (git 11e1ce2) shows the actual failure class is un-wired standalone gates (openapi-lint went stale for a whole re-snapshot), and the fix direction was more wiring, not fewer gates.",
      "contradictoryEvidence": [
        "Makefile:147 perfect-fast includes agent-handoff; Makefile:153-157 contract-gates bundles 64 leaves; Makefile:159 governance-contracts 21 leaves; Makefile:162 release-proof 3+ leaves",
        "Makefile:171-179 aggregate-gates runs 7 test suites + check-aggregate-gates.mjs + check-test-wiring.mjs",
        "docs/live-evidence-currentness.json inputHashes.Makefile SHA-256 pin; quality-gates.md:32 documents openapi-lint's standalone rot and the Makefile-as-governed-input constraint",
        "git 11e1ce2: openapi-lint pinned 163 while surface was 168 and stayed invisible 'because no aggregate reaches the target'; enterprise-audit stale marker; aggregate-gates missing verify-plan sequence entry",
        "docs/gate-tier-inventory.json rows regenerate from the Makefile (generate-gate-tier-inventory.mjs); gate-tier-inventory-drift fails on mismatch",
        "scripts/check-contract-inventory.mjs (contract-inventory target, Makefile:497) verifies every gate's checker/contract registration"
      ],
      "supportingEvidence": [
        "git 11e1ce2 and fa1673c show gates catching real regressions: version-consistency (release 1.0.1), official-openapi-drift (161/168), consumer-cast-budget (1,463-case proof, risk-register.json:381), mutation-score (49462f5, 80f48bb)",
        "Gates that protect shipped behavior (version-consistency, sdk-public-api, package-contract, pack-snapshot-check) run in release workflows (release.yml, ci-cli-release.yml, ci-mcp-release.yml)"
      ],
      "hiddenConsumers": [
        "The Makefile text itself is consumed by checkers that assert on literal 'node --test scripts/<name>.test.mjs' substrings (docs/test-wiring-contract.json purpose: consumer-cast-contract.mjs, check-operation-coverage.mjs, check-release-dispatch-guard.mjs, check-mcp-release-workflow.test.mjs)",
        "../GOCLMCP/Makefile is consumed via Makefile:249-252 goclmcp-drift and docs/aggregate-gates-goclmcp.Makefile fallback (check-aggregate-gates.mjs expectedFallbacks)",
        "scripts/live/run-live-evidence-campaign.mjs records the Makefile hash into docs/live-evidence-currentness.json"
      ],
      "generatedOwnership": "docs/gate-tier-inventory.json/.md, docs/quality-gates.md regenerated from the Makefile",
      "compatibilityConstraints": [
        "aggregate-gates exact-once contract (docs/aggregate-gates-contract.json)",
        "live-evidence currentness inputHashes (Makefile, package.json, wrapper build files)",
        "gate-tier-inventory-drift + contract-inventory + docs-counts + enterprise-audit all register targets"
      ],
      "intentionalDuplication": [
        "None - the Makefile already centralizes leaf lists in verify.mjs and the aggregates"
      ],
      "smallestValidRemediation": "Leave alone; the one real fix (wiring standalone openapi-lint into product-contracts) already landed in fa1673c/11e1ce2. Any target merge requires coordinated regeneration of gate-tier-inventory, contract-inventory, quality-gates.md, and a live-evidence campaign re-run.",
      "confidence": "high"
    },
    {
      "candidateId": "H5",
      "verdict": "rejected",
      "challengeSummary": "consumer-cast-budget is a TypeScript-compiler-aware dataflow proof (scripts/lib/consumer-cast-governance.mjs: 12,951 lines; test suite: 13,124 lines), not a lintable pattern; it was motivated by real escape-hatch request casts in CLI/MCP write paths (git b1b4258, 900da3a, 2abd367; docs/roadmap-1.0-receipts/task-07-zero-cast.md; discrepancies.md:2663-2700) and caught 2 residual MCP holiday request assertions on its own. An eslint rule is syntactic and cannot prove generated-type provenance through helpers/spreads/destructuring; the contract pins zero exceptions at 1.0 (docs/consumer-cast-budget-contract.json requestCastGovernance.canonicalZeroBaseline).",
      "contradictoryEvidence": [
        "docs/roadmap-1.0-receipts/task-07-zero-cast.md: 'The source scan found and removed two residual MCP holiday request-object assertions'",
        "discrepancies.md:2663-2700 documents the cast-removal progression (projects.list, clients.list, expenses...) and the resolution record; risk-register.json:372-384 'consumer-request-casts' risk closed 2026-07-21 by the 1,463-case proof",
        "docs/consumer-cast-budget-contract.json publicNoAnyProof requires exact adapter import provenance + type arguments + unshadowed built-ins - a compile-level assertion, not lint",
        "git b1b4258 touched cli/src/scope-filter.ts, mcp/src/tools/holidays.ts removing actual casts; 11e1ce2-style gate rot is prevented because consumer-cast-budget-run is in release-proof (Makefile:162)"
      ],
      "supportingEvidence": [
        "The engine's scope is documented exhaustively in task-07-zero-cast.md (bounded variable aliases, helper calls, spreads, call/apply/bind, any-erased receivers, Function trampolines...) - these are the escape routes a lint rule cannot see",
        "Wrapper tests/types/breaking-changes.test-d.ts proves adapter callback inputs are not any via the compiler (publicNoAnyProof.path)"
      ],
      "hiddenConsumers": [
        "make consumer-cast-budget-run -> Makefile:162 release-proof -> release workflows (release.yml, ci-cli-release.yml, ci-mcp-release.yml run make release-proof)",
        "docs/consumer-cast-budget-contract.json is also consumed by wrapper/tsconfig.json (compilerGate wiring)"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "Canonical 1.0 baseline requires CLI and MCP exception arrays exactly empty (contract purpose text)",
        "Risk-register closure: exceptions require open risk + discrepancy + evidence + exact closure gate"
      ],
      "intentionalDuplication": [
        "None - the engine is the only implementation of this proof in-repo"
      ],
      "smallestValidRemediation": "Leave alone; if maintenance cost is ever a concern, extend the 1,463-case suite only when a new escape-hatch class appears - never replace the engine with lint.",
      "confidence": "high"
    },
    {
      "candidateId": "H6",
      "verdict": "narrowed",
      "challengeSummary": "Duplication is real but mostly generated or boundary-intentional. The 298-line error-codes.ts in cli/ and mcp/ are byte-identical because scripts/generate-error-docs.mjs:11-16 emits the same file into all three packages from docs/error-codes.json - already single-sourced. The hand-written buildRoutingOptions/REGIONAL_PREFIXES/KNOWN_REGIONS in cli/src/client.ts vs mcp/src/client.ts (~40 lines each) differ in UX error strings (CLI flags vs MCP env vars), client construction (CLI lazy-imports the SDK root and prints an unconfirmed-region notice; MCP has a setup_required MissingCredentialsError flow), and release cadence (independent packages with peerDependencies on the SDK ^1). Centralizing would require a new frozen wrapper subpath at 1.x + size-limit ceiling + verify-dual-build names entry + peer-floor bumps for both consumers.",
      "contradictoryEvidence": [
        "cli/src/error-codes.ts and mcp/src/error-codes.ts: diff is empty; both carry the generated header; docs/error-registry-contract.json:35-37,56 registers all three copies as mirrors",
        "cli/src/client.ts:13-21 vs mcp/src/client.ts:14-21: identical REGIONAL_PREFIXES/KNOWN_REGIONS; cli/src/client.ts:24-58 vs mcp/src/client.ts:23-49: buildRoutingOptions with different error strings ('--subdomain requires --region' vs 'CLOCKIFY_SUBDOMAIN requires CLOCKIFY_REGION')",
        "cli/src/client.ts:73-78 lazy-imports the SDK root; mcp/src/client.ts:52-78 has MissingCredentialsError + buildSetupMessage for the MCP env-block UX",
        "mcp/src/orchestration/confirmation.ts (146 lines) - MCP-specific confirmation token flow with no CLI analog; cli/src/output.ts (208 lines) table formatting with no MCP analog; mcp/src/result.ts (526 lines) envelope model vs CLI receipts",
        "cli/package.json and mcp/package.json are independent publishes with peerDependencies 'clockify-sdk-ts-115': '^1' and history of deliberate peer-floor bumps (git 97a0ad2)"
      ],
      "supportingEvidence": [
        "AGENTS.md:396-401 documents the cost of adding a wrapper root module (tsconfig include + exports both conditions + verify-dual-build.sh expected-names array + type-check/test/build/smoke/pack)",
        "docs/one-point-zero-classification.json: any new subpath joins the frozen 1.0 surface; docs/sdk-public-api.json pins the current surface"
      ],
      "hiddenConsumers": [
        "None beyond the two packages; no cross-package imports exist (grep of cli/src, mcp/src for sibling-package imports: none; only clockify-sdk-ts-115 imports)"
      ],
      "generatedOwnership": "cli/src/error-codes.ts, mcp/src/error-codes.ts, wrapper/error-codes.ts are generated by scripts/generate-error-docs.mjs (make error-docs)",
      "compatibilityConstraints": [
        "Independent release cadence (3 tags, 3 tarballs, peerDependency floors)",
        "1.x frozen public surface for the SDK",
        "Error-registry contract registers all three emitted copies"
      ],
      "intentionalDuplication": [
        "error-codes triplication - generator emission so each independently-published package carries its own table",
        "buildRoutingOptions - UX-boundary-specific error strings and input channels (argv vs env) with acknowledgeUnconfirmedRegion as the documented 'deliberate act' (cli/src/client.ts:26-30, mcp/src/client.ts:25-29)"
      ],
      "smallestValidRemediation": "Leave alone; the error-codes duplication is already single-sourced through the generator, and a wrapper subpath to save ~40 lines of argv/env parsing is a net loss at 1.x.",
      "confidence": "medium"
    },
    {
      "candidateId": "H7",
      "verdict": "rejected",
      "challengeSummary": "AGENTS.md (821 lines) and CLAUDE.md (250 lines) are contract-pinned: docs/agent-handoff-contract.json requires exact mustContain strings in both files, enforced by scripts/check-agent-handoff.mjs under make agent-handoff, which is in perfect-fast (Makefile:147) and governance-contracts. The shrink already happened: CLAUDE.md's 9 gotcha subsections (357 lines) were moved to docs/gotchas/ (9 files) on 2026-08-01 (.remember/today-2026-08-01.done.md:6). AGENTS.md section 4 encodes load-bearing operational invariants (mutation is GitHub-only, 30-module count, wrapper/src wiped by sync, perf-budget flake caveats, four drift gates after generator changes).",
      "contradictoryEvidence": [
        "docs/agent-handoff-contract.json guidance[0].mustContain for AGENTS.md (11 strings incl. 'Never edit `spec/corrected/...`') and guidance[1].mustContain for CLAUDE.md (7 strings incl. 'No `npm publish`')",
        "Makefile:147 perfect-fast lists agent-handoff; Makefile:159 governance-contracts lists agent-handoff; Makefile:166-169 governance-audit/perfect-full reach it",
        ".remember/today-2026-08-01.done.md:6 documents the gotcha consolidation with gates green - the shrink was already executed and registered",
        "AGENTS.md:22-64 (checkpoint) encodes the SSRF mutation regression story - non-obvious operational truth not duplicated in docs/",
        "AGENTS.md:345-414 (section 4) encodes the exact gate-per-change-scope table (generator changes, wrapper/src prohibition, 30-module rule, verify-dual-build expected-names) - duplicated nowhere else"
      ],
      "supportingEvidence": [
        "CLAUDE.md:199-227 now points to 9 docs/gotchas files and keeps only 2 load-bearing facts inline - the consolidation pattern the hypothesis asks for already exists",
        "docs/agent-handoff-policy.md mustContain list anchors the policy document"
      ],
      "hiddenConsumers": [
        "scripts/check-agent-handoff.mjs (reads docs/agent-handoff-contract.json and scans AGENTS.md/CLAUDE.md/README.md text)",
        "scripts/check-agent-tasks.mjs (five-anchor wiring incl. docs/README.md index)",
        "AGENTS.md:368-373 instructs operators to use node scripts/plan.mjs topics - shrinking that table would orphan the plan module registry"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "agent-handoff gate mustContain strings; forbiddenMarkers TODO/TBD (docs/agent-handoff-contract.json)",
        "docs-index-drift + docs-counts register AGENTS.md/CLAUDE.md-related surfaces"
      ],
      "intentionalDuplication": [
        "AGENTS.md vs CLAUDE.md overlap (verify gates, hard stops) - deliberate: AGENTS.md is the canonical contributor contract (mustContain 'Canonical contributor + agent contract'), CLAUDE.md is the session-optimized view; the handoff contract pins both"
      ],
      "smallestValidRemediation": "Leave alone; any drastic shrink must first edit docs/agent-handoff-contract.json and re-register, and the consolidation the hypothesis proposes was already completed on 2026-08-01.",
      "confidence": "high"
    },
    {
      "candidateId": "H8",
      "verdict": "rejected",
      "challengeSummary": "The mutation machinery is the only gate that caught the webhook-url SSRF regression (AGENTS.md:22-64: lost ~9.5 points, 14 uncovered mutants, 'while every other gate stayed green'), and it runs only on GitHub workflow_dispatch (make mutation refuses locally, Makefile mutation target), so its CI cost is zero; its maintenance surface is 3 stryker configs + ~10 checker files + 3 contract docs. Floors are monotonic ratchets validated across complete first-parent history (docs/mutation-score-contract.json purpose).",
      "contradictoryEvidence": [
        "AGENTS.md:22-64: 'Between 2026-07-31 and 2026-08-05 the SSRF guard wrapper/webhook-url.ts lost ~9.5 points and gained 14 mutants with no test coverage at all, while every other gate stayed green'",
        "git 49462f5 'kill the three mutants the 1.0.1 mutation run exposed'; git 80f48bb 'close the webhook-url mutation holes'; git 9b56ebe '5 unproven mutation leaves'",
        "Makefile mutation target: '@test \"$$GITHUB_ACTIONS\" = true' guard - local runs impossible by design",
        "docs/mutation-score-contract.json:22 pins wrapper/webhook-url.ts floor 94; the checker validates every contract-changing commit and the sole immutable retirement (0392e69 confirm-guard.ts -> tool-risk.ts, floor 70)",
        "mutation.yml is workflow_dispatch-only; check-mutation-ci-workflow.mjs + mutation-ci gate verify the wiring offline"
      ],
      "supportingEvidence": [
        "Cost inventory: wrapper/stryker.conf.json, cli/stryker.conf.json, mcp/stryker.conf.json; scripts/check-mutation-score.mjs + .e2e.test.mjs; scripts/check-mutation-ci-workflow.mjs + test; scripts/lib/mutation-score*.mjs; scripts/check-remote-mutation-proof-contract.mjs; docs/mutation-safety-contract.json + policy, docs/mutation-score-contract.json, docs/remote-mutation-proof-contract.json; 5 roadmap receipts (task-14..18)",
        "cli/tests/mutation-leaves.test.ts + mcp pinned governed-module tests prevent Vitest discovery gaps (mutation-score-contract purpose)"
      ],
      "hiddenConsumers": [
        "check-mutation-score.mjs:274 LEGACY_MODULE_REPLACEMENT pins that mcp/src/orchestration/confirm-guard.ts never reappears",
        "mutation-ci runs in perfect-fast (Makefile:147) and security-contracts (Makefile:154)"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "Module floors map 1:1 to Stryker mutate lists; adding a module requires GitHub-measured floor (AGENTS.md:22-64)"
      ],
      "intentionalDuplication": [
        "None - the GitHub-only execution is the deliberate constraint (local runs overwrite graded reports with wrong scores, AGENTS.md:22-64)"
      ],
      "smallestValidRemediation": "Leave alone; removal deletes the only signal that caught the SSRF regression, and the machinery costs no CI time.",
      "confidence": "high"
    },
    {
      "candidateId": "H9",
      "verdict": "rejected",
      "challengeSummary": "The release-please WORKFLOW is retired (git c5e7b97) and its non-return is pinned by check-ci-contract.mjs + docs/risk-register.json:205-213 + docs/ci-contract.json:249-250, but release-please-config.json and .release-please-manifest.json are live gate inputs: scripts/check-version-consistency.mjs:145-177 reads both and fails if package versions or package-name mappings drift; the gate is in release-contracts (Makefile:156) -> contract-gates -> all four release workflows.",
      "contradictoryEvidence": [
        "scripts/check-version-consistency.mjs:145-152 reads policy.releasePleaseManifest and policy.releasePleaseConfig; lines 153-177 fail on missing tracked keys/package-name mismatches",
        "scripts/check-version-consistency.test.mjs:57-58,129-130,181-303 asserts mutations of both files fail the check",
        "docs/version-policy.json:74-76 names both files as required manifests",
        "docs/risk-register.json:205: 'release-please.yml was retired... check-ci-contract.mjs now fails if the workflow returns. The tag-vs-version guard remains load-bearing.'",
        ".claude/skills/clockify-sdk-publish/SKILL.md:39: 'release-please is retired' - the skill instructs using version-consistency, which reads the manifest"
      ],
      "supportingEvidence": [
        "Makefile:74 help text: 'make version-consistency Check package versions against release-please manifest policy.'",
        "AGENTS.md:22-64: 'make version-consistency reconciles all three package manifests with the retained .release-please-manifest.json'"
      ],
      "hiddenConsumers": [
        "scripts/check-version-consistency.mjs (release-contracts, CI)",
        "scripts/check-version-consistency.test.mjs (mutation fixtures of both files)",
        "docs/version-policy.json (policy source)",
        "Makefile help text"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "version-consistency gate: manifest tracks each package version; config maps package ids to npm names",
        "ci-contract pins the retired workflow must not return"
      ],
      "intentionalDuplication": [
        "The retained manifest/config duplicate package.json version data - deliberate: they are the frozen gate record that survived the workflow retirement"
      ],
      "smallestValidRemediation": "Leave alone; the files are live inputs to version-consistency, and their deletion reds release-contracts in every CI release workflow.",
      "confidence": "high"
    },
    {
      "candidateId": "H10",
      "verdict": "rejected",
      "challengeSummary": "The 28 exports entries (root + 27 subpaths, wrapper/package.json) are a frozen 1.0 surface with measured evidence for keeping every subpath: docs/one-point-zero-classification.json notes document the keep-all-28 decision (each of the 13 single-symbol subpaths has at least one importer; wrapper/.size-limit.json holds per-subpath byte ceilings; observability trio health 1.2kB / otel-hooks 4.0kB / diagnostics 8.2kB - folding grows a health-only import to 13.4kB and CJS cannot tree-shake). Removing any subpath at 1.x is a breaking change for published consumers; the documented path is deprecation, not deletion.",
      "contradictoryEvidence": [
        "wrapper/package.json exports: 28 entries incl. root; files: [dist, README.md, LICENSE]",
        "docs/one-point-zero-classification.json notes (2026-08-05): 'All 28 subpaths are kept at 1.0, including the 13 that export exactly one symbol... merging them was weighed and rejected on measured evidence'",
        "git 638f9de 'docs(1.0): record why all 28 SDK subpaths are kept' (13 subpaths have importers in wrapper/examples, wrapper/tests, cli/src, mcp/src, wrapper/README.md; per-subpath byte ceilings deliberate)",
        "wrapper/.size-limit.json: per-subpath ceilings (create-client 4kB, composed-fetch 7.6kB, root ESM 2kB, CJS 3.7kB)",
        "docs/sdk-public-api.json (366 lines) pins rootSymbols + subpaths; consumed by check-sdk-public-api.mjs, wrapper/scripts/verify-dual-build.sh, wrapper/tsconfig.json self-reference aliases",
        "wrapper/scripts/verify-dual-build.sh pins the 93-name curated surface + GENERATED_CORE leak list ('frozen + gated rather than implicit + unchecked')"
      ],
      "supportingEvidence": [
        "docs/one-point-zero-classification.json approvedDecisions include 'deprecated' - the sanctioned removal path exists and requires classification, not ad-hoc deletion",
        "CLI/MCP import 17 distinct SDK subpaths (grep count: requests 45, resolve 11, operation-receipt 5, iter 4, ...) - the surface is exercised"
      ],
      "hiddenConsumers": [
        "wrapper/tsconfig.json self-reference aliases; wrapper/scripts/verify-dual-build.sh expected-names; docs/sdk-public-api.json; wrapper/.size-limit.json; examples and tests importing subpaths"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "1.x semver freeze; sdk-public-api drift gate; verify-dual-build smoke in build:smoke and CI",
        "CJS consumers cannot tree-shake - per-subpath granularity is the only size lever"
      ],
      "intentionalDuplication": [
        "Single-symbol subpaths (composed-fetch, health, iter, ...) - deliberate granularity with byte ceilings, not accidental fragmentation"
      ],
      "smallestValidRemediation": "Leave alone at 1.x; if a subpath ever becomes unused, follow the documented deprecation classification at 2.0 - none is unused today.",
      "confidence": "high"
    },
    {
      "candidateId": "H11",
      "verdict": "narrowed",
      "challengeSummary": "Per-package builds with twin tsc for the wrapper are load-bearing: the wrapper must ship dual ESM/CJS under a 'type':'module' root (finalize-cjs.sh writes dist/cjs/package.json {'type':'commonjs'}), the mcp package deliberately ships ESM-only (exports conditions have no require branch), and cli ships a single bin entry. The wrapper's 5 tsconfigs serve distinct gates (EOPT+noImplicitOverride for consumer types; types-bundler and types-public-package for breaking-change proofs). Unification would force CJS onto mcp/cli (surface doubling) or add conditional machinery, and would couple release timing across three independently published packages. The right unification seam already exists: the root build script orchestrates the three package builds, and generate-package-versions.mjs is the shared pre-build step.",
      "contradictoryEvidence": [
        "wrapper/package.json build: 'tsc -p tsconfig.esm.json && tsc -p tsconfig.cjs.json && bash scripts/finalize-cjs.sh'; finalize-cjs.sh writes {'type':'commonjs'} into dist/cjs",
        "wrapper/scripts/verify-dual-build.sh: ESM + CJS import smokes against the 93-name pinned surface",
        "mcp/package.json exports: only types+default per condition (no require branch); cli/package.json exports '.' -> dist/index.js (ESM bin)",
        "wrapper/tsconfig.types-bundler.json + tsconfig.types-public-package.json feed type-check:breaking (breaking-changes.test-d.ts, public no-any proofs)",
        "Wrapper docs: CJS cannot tree-shake (one-point-zero-classification notes) - dual format is the reason for per-subpath granularity, not an accident",
        "cli/package.json and mcp/package.json builds are single tsc -p tsconfig.build.json; their mutation and test scripts build the SDK first (workspace dependency) - the coupling that exists is already explicit"
      ],
      "supportingEvidence": [
        "Root package.json scripts already orchestrate: type-check/build/test iterate the three workspaces - the 'unified' entrypoint exists",
        "release.yml/ci-cli-release.yml/ci-mcp-release.yml build the wrapper first, then each package - per-package release independence is the design",
        "3 stryker configs + 3 vitest configs + per-package size budgets (cli entrypoint size budget recalibrated in git e271812) argue for per-package build identity"
      ],
      "hiddenConsumers": [
        "scripts/generate-package-versions.mjs runs before every package build/test/type-check (all 3 package.json script sets) - the shared step is already centralized",
        "verify-dual-build.sh is invoked by wrapper build:smoke and CI (ci.yml: build:smoke step)"
      ],
      "generatedOwnership": "none",
      "compatibilityConstraints": [
        "Dual-package publishing requires the type:commonjs marker file inside dist/cjs",
        "mcp exports shape (no require) is a published contract",
        "wrapper EOPT+noImplicitOverride are consumer-visible type settings (git 14a471a)"
      ],
      "intentionalDuplication": [
        "Three package builds for three tarballs - the release independence is the point; the wrapper's dual build is the only dual-format requirement"
      ],
      "smallestValidRemediation": "Leave alone; keep per-package builds and the root orchestration; do not force a unified single-build pipeline onto packages with different format obligations.",
      "confidence": "medium"
    }
  ],
  "leaveAloneList": [
    {
      "structure": "wrapper/internal/routing.ts hand-copy of docs/service-routing-matrix.json",
      "why": "Policy-documented evidence/runtime boundary; JSON cannot be imported from the published tarball; equality test checks both drift directions (docs/service-routing-matrix-policy.md:8-12, wrapper/tests/routing-matrix-equality.test.ts:88-119)"
    },
    {
      "structure": "cli/src/error-codes.ts + mcp/src/error-codes.ts identical files",
      "why": "Generator emission from docs/error-codes.json (scripts/generate-error-docs.mjs:11-16); each independently-published package needs its own copy; single source already exists"
    },
    {
      "structure": ".release-please-manifest.json + release-please-config.json after workflow retirement",
      "why": "Live inputs to check-version-consistency.mjs:145-177 (release-contracts gate in CI); the risk-register records the retirement and the guard that blocks the workflow's return"
    },
    {
      "structure": "output/scratch-*.mjs (12 files)",
      "why": "Unreferenced evidence probes; small, tracked as evidence history (commit a8d61d0), zero gate impact; deleting is safe but pointless"
    },
    {
      "structure": "docs/operation-parity.json (3,223 lines)",
      "why": "Drives a runtime in-memory MCP test (mcp/tests/operation-parity.behavioral.test.ts:14) and docs-counts authoritative pointers"
    },
    {
      "structure": "Quarantined tests scripts/check-aggregate-wiring.test.mjs and scripts/check-mcp-write-safety.test.mjs",
      "why": "Deliberately unwired with documented dispositions (in-place tracked-file rewrites) in docs/test-wiring-contract.json; the meta-gate allows them only via that contract"
    },
    {
      "structure": "GENERATED_CORE leak list in wrapper/scripts/verify-dual-build.sh",
      "why": "Deliberate 'frozen + gated rather than implicit + unchecked' pin of transitive root-barrel leaks"
    },
    {
      "structure": "check-enterprise-hardening.retired-receipts.test.mjs and the confirm-guard.ts non-reappearance pin (check-mutation-score.mjs:274)",
      "why": "Retired-path guards: the retired branch was a latent false-green; the repo pins that retired code never reappears"
    },
    {
      "structure": "cli/src/client.ts vs mcp/src/client.ts buildRoutingOptions",
      "why": "UX-boundary duplication (~40 lines) with different input channels and error strings; centralizing costs a frozen 1.x subpath + peer-floor bumps"
    },
    {
      "structure": "AGENTS.md 821 lines",
      "why": "Contract-pinned by docs/agent-handoff-contract.json mustContain strings under make agent-handoff (perfect-fast); already consolidated once (gotchas moved to docs/gotchas/ on 2026-08-01)"
    }
  ],
  "commandsExecuted": [
    {"command": "git rev-parse HEAD && git status --short", "exitCode": 0, "stdout": "931ac0114fe98211f96f82b0a3e76b145b261150; ?? .ai-audit/ ?? .pi-subagents/", "stderr": ""},
    {"command": "git ls-files | awk file-count by top dir", "exitCode": 0, "stdout": "scripts 264, docs 242, wrapper 180, mcp 165, cli 96, spec 19, total 1009", "stderr": ""},
    {"command": "grep -c '^[a-zA-Z0-9_.-]*:' Makefile", "exitCode": 0, "stdout": "179", "stderr": ""},
    {"command": "python3 count of wrapper/package.json exports", "exitCode": 0, "stdout": "28 entries; files [dist, README.md, LICENSE]", "stderr": ""},
    {"command": "diff cli/src/error-codes.ts mcp/src/error-codes.ts", "exitCode": 0, "stdout": "no output (identical)", "stderr": ""},
    {"command": "grep -rl per-docs-JSON consumer scan (130 JSONs)", "exitCode": 0, "stdout": "all docs/*.json except live-differential-receipt.json have >=1 script/CI consumer", "stderr": ""},
    {"command": "git log --oneline -- release-please-config.json .release-please-manifest.json", "exitCode": 0, "stdout": "fa1673c, 54e7075, e574371, 97a0ad2, ... (release history)", "stderr": ""},
    {"command": "git tag | grep -E '1\\.0\\.1|v1\\.0'", "exitCode": 1, "stdout": "cli-v1.0.0 mcp-v1.0.0 wrapper-v1.0.0 (no 1.0.1 tags exist locally)", "stderr": ""},
    {"command": "grep -rn 'verify-remote-mutation-proof|check-aggregate-wiring' Makefile .github scripts .claude", "exitCode": 1, "stdout": "no matches (expected; both are explained by docs/test-wiring-contract.json quarantine or lib imports)", "stderr": ""},
    {"command": "git show 11e1ce2 (four gates that had stopped guarding)", "exitCode": 0, "stdout": "openapi-lint pin 163 vs 168; enterprise-audit marker; aggregate-gates sequence entry", "stderr": ""},
    {"command": "git show 638f9de (why all 28 SDK subpaths are kept)", "exitCode": 0, "stdout": "13 single-symbol subpaths each with >=1 importer; size-limit ceilings; CJS no tree-shake", "stderr": ""},
    {"command": "python3 gate-tier-inventory/contract-inventory counts", "exitCode": 0, "stdout": "87 decision rows + 4 aggregate rows; 93 contract entries + retiredGates", "stderr": ""},
    {"command": "wc -l scripts/lib/consumer-cast-governance.mjs scripts/check-consumer-cast-budget.test.mjs", "exitCode": 0, "stdout": "12951 / 13124", "stderr": ""},
    {"command": "git check-ignore wrapper/src/index.ts", "exitCode": 0, "stdout": "wrapper/src/index.ts (ignored; generated)", "stderr": ""},
    {"command": "grep -rn 'scratch-' Makefile .github scripts wrapper cli mcp docs", "exitCode": 1, "stdout": "no matches (output/scratch-*.mjs unreferenced)", "stderr": ""}
  ],
  "receipts": ["inline - all evidence captured in this report with file:line citations"],
  "uncertainties": [
    "Whether 1.0.1 was actually published to npm: no 1.0.1 git tags exist locally (38 tags, latest v1.0.0); package.json + manifest say 1.0.1; offline read-only run cannot verify npm state (requires_external_verification)",
    "Whether scripts/lib/consumer-cast-governance.mjs (12,951 lines) contains dead branches: I did not execute the engine (requires built SDK); any internal deadness claim is UNVERIFIED",
    "../GOCLMCP cross-repo direction verified only one way (GOCLMCP -> this repo via goclmcp-drift/spec-sync-drift); whether GOCLMCP's CI consumes this repo's docs JSONs is outside this repo and UNVERIFIED",
    "The exact identity of the fourth gate in 11e1ce2's 'four gates' (three are named in the visible message)"
  ]
}
```

---

# Architectural Falsifier Report

**Falsifier ID:** architectural-falsifier (read-only DeepSeek Flash subagent)
**Repository:** `/Users/15x/Downloads/WORKING/addons-me/clockify-ts-sdk`
**Head:** `931ac0114fe98211f96f82b0a3e76b145b261150` (main)
**Worktree:** clean except untracked `.ai-audit/` and `.pi-subagents/` (harness artifacts; nothing staged)
**Sister repo:** `../GOCLMCP` EXISTS locally (`Makefile`, `scripts/gen-clockify-openapi` verified) — cross-repo gates are live.

---

## Phase 1 — Independent observations (recorded before evaluating the hypotheses)

1. **The "~40 gates" premise is wrong by 4.5×.** `grep -c '^[a-zA-Z0-9_.-]*:' Makefile` = **179 targets** (`.PHONY` at Makefile:1-4). They cluster into ~6 aggregates: `perfect-fast` (20 leaves, Makefile:147), `product-contracts` (40), `security-contracts` (8), `release-contracts` (7), `docs-contracts` (9), `governance-contracts` (21), `release-proof` (Makefile:162), `perfect-full` (Makefile:168).
2. **Nothing from `docs/`, `scripts/`, or `wrapper/internal/` ships to npm.** All three packages declare `"files": ["dist", "README.md", "LICENSE"]`. This single fact decides H3 and shapes H2/H9.
3. **`wrapper/src` is not tracked.** `wrapper/.gitignore` ignores `src/`; `git check-ignore wrapper/src/index.ts` confirms. The generated core exists only after `make sdk-codegen`. The hand-written boundary is exactly: 30 root `.ts` modules + 4 `wrapper/internal/*.ts` (tracked counts verified).
4. **The Makefile is itself a governed, hash-pinned input.** `docs/live-evidence-currentness.json` `inputHashes` contains `"Makefile": "70985c4c…"`. Any Makefile edit forces a live-evidence campaign re-run (documented in `docs/quality-gates.md:32`). This constrains H4 directly.
5. **The repo already has machinery to find dead tests** (`scripts/check-test-wiring.mjs`, wired into `aggregate-gates`, Makefile:179). Its contract (`docs/test-wiring-contract.json`) records that **24 of 51 test files were once executed by nothing** — the meta-gate exists because silent unwiring actually happened. Two tests are *quarantined with documented reasons*, not dead.
6. **`error-codes.ts` triplication is generated, not authored.** `cli/src/error-codes.ts` and `mcp/src/error-codes.ts` are byte-identical (empty `diff`) and both carry `Generated by scripts/generate-error-docs.mjs` headers; the generator (scripts/generate-error-docs.mjs:11-16) emits `wrapper/error-codes.ts`, `cli/src/error-codes.ts`, `mcp/src/error-codes.ts` from `docs/error-codes.json`.
7. **Agent guidance is contract-pinned.** `docs/agent-handoff-contract.json` requires exact `mustContain` strings in AGENTS.md and CLAUDE.md, enforced by `scripts/check-agent-handoff.mjs` under `make agent-handoff`, which is inside `perfect-fast` (Makefile:147).
8. **The docs JSON web is load-bearing, including at runtime.** `mcp/tests/operation-parity.behavioral.test.ts:14` boots the real MCP server in-memory and asserts every operation stamped in `docs/operation-parity.json` resolves to a registered tool. Deleting that JSON breaks a runtime test.
9. **`11e1ce2` documents the repo's own failure class:** four gates had stopped guarding (openapi-lint pinned at 163 operations while the surface was 168; enterprise-audit stale marker; aggregate-gates missing sequence entry) — because a gate no aggregate reaches rots invisibly. The fix was **more wiring**, not fewer gates.
10. **The mutation gate is the only signal that caught the SSRF regression** (AGENTS.md:22-64): `wrapper/webhook-url.ts` lost ~9.5 points and gained 14 uncovered mutants between 2026-07-31 and 2026-08-05 "while every other gate stayed green."
11. **1.0.1 has no local git tags.** `git tag` shows newest per package = `*-v1.0.0`; HEAD is 16 commits after `wrapper-v1.0.0`. `package.json`/`.release-please-manifest.json` say 1.0.1. npm publication state is **UNVERIFIED** offline.
12. **`output/scratch-*.mjs` (12 files) is the only truly unreferenced tracked cluster** — zero matches in Makefile, CI, scripts, or docs. They are small evidence probes (last touched by `a8d61d0`).

---

## Section A — Per-hypothesis verdicts

### H1 — "scripts/ has 264 files; most are dead or one-off" → **REFUTED**

Count is right (264 tracked), deadness is wrong. Of 261 tracked `.mjs` files, **197 are invoked by literal path** in Makefile/workflows/package.json (`grep -c` over `scripts/<name>` patterns). The remainder are:
- **Libraries imported by checkers:** `budget-fingerprint.mjs` (imported by `check-performance-budgets.mjs:6`), `readiness-test-fixtures.mjs` (`check-release-readiness.mjs:8`), `roadmap-status-contract.mjs` (`check-risk-register.mjs:13`), `mcpb-artifacts.mjs` (`build-mcpb.mjs:47`, `smoke-mcpb.mjs:29`), `create-support-bundle.mjs` (`check-support-bundle.mjs:7`), `build-replay-fixtures.mjs` (invoked by `probe-and-stamp.mjs:69`).
- **Plan modules consumed only via `node scripts/plan.mjs <topic>`** (10 files: acceptance-plan, change-impact-plan, examples-plan, maintenance-plan, onboarding-plan, workflow-plan, performance-calibration-plan, release-decision-plan, contract-inventory-report, risk-status-report — registry at `scripts/plan.mjs:13-22`).
- **Two quarantined tests** (`check-aggregate-wiring.test.mjs`, `check-mcp-write-safety.test.mjs`) — explicitly unwired **by contract** (`docs/test-wiring-contract.json`), because they rewrite tracked files in place and a crash would red unrelated git-diff gates. Documented disposition, not deadness.
- **Subdir files** (`lib/` 41, `live/` 12, `sdk-codegen/` 12) — all imported by top-level checkers; `scripts/lib/aggregate-gates.test.mjs` is named directly in Makefile:172.

Deletion history (`git log --diff-filter=D`) shows the repo actively prunes (`find-doc-integrity.mjs`, `sync-sdk.sh`, `deno-smoke.ts`, `release-please.yml`). The meta-gate `check-test-wiring` fails on any test wired to nothing, so genuine deadness is already caught.

**Strongest reason to leave alone:** the only dead-looking cluster (`output/scratch-*`, 12 files) is outside `scripts/`, harmless, and historically load-bearing as evidence; everything inside `scripts/` has a live consumer or a documented quarantine.

### H2 — "docs/ JSON contracts/ledgers are self-satisfying bureaucracy" → **REFUTED**

I measured consumers for every docs JSON: **every one except `docs/live-differential-receipt.json` has ≥1 consumer** in scripts/wrapper/cli/mcp/.github/Makefile (top consumers: contract-inventory 61, enterprise-hardening-audit 59, risk-register 19, openapi-operations 20, error-codes 17, product-surface 13). The exception is a **gate output**: `scripts/live/check-live-differential.mjs:167-169` writes the receipt, `docs/live-differential-contract.json:30` names it as `receiptPath`, and `docs/README.md:158` documents it.

The decisive runtime consumer: `mcp/tests/operation-parity.behavioral.test.ts:14` reads `docs/operation-parity.json` and asserts every stamped operation resolves to a real MCP tool and SDK method (in-memory server boot). Deleting the JSON breaks a test that runs in CI.

Deleting *any* indexed JSON additionally reds:
- `docs-index-drift` — `docs/docs-index-contract.json` pins `expectedRequiredLinkCount: 139` in `docs/README.md`;
- `docs-counts` — `docs/docs-counts-contract.json` authoritative pointers live inside `openapi-operations.json`, `operation-parity.json`, `mcp-tools.json`;
- `contract-inventory` (93 entries, `docs/contract-inventory.json`) and `gate-tier-inventory` (87 decision rows + 4 aggregate rows, `docs/gate-tier-inventory.json`).

**Strongest reason to leave alone:** the JSON files are the machine-checkable half of a deliberate contract+policy+checker pattern where each gate's contract is registered in four inventories; consolidation would require regenerating the whole ledger web simultaneously.

### H3 — "routing.ts hand-mirrors the matrix; single-source it" → **REFUTED**

The premise is inverted: the repo *already* documents why single-sourcing is impossible.
- `docs/service-routing-matrix.json:6-8`: "No runtime code imports this file — wrapper/internal/routing.ts keeps an equality-pinned hand-written copy…"
- `docs/service-routing-matrix-policy.md:8-12`: "**No runtime code imports this file.** It stays evidence tooling."
- **The published tarball cannot import the JSON**: `wrapper/package.json` `files: ["dist","README.md","LICENSE"]`. A consumer install has no `docs/`. Runtime import would require either shipping `docs/` (tarball + surface change) or generating `routing.ts` at build time (turning a hand-written file into generated output — the codegen chain currently regenerates only `wrapper/src`, and `check-no-generated-edits.mjs` pins exactly three generated paths).
- The JSON is an **approval artifact with provenance** (`approvedBy: apet97`, `approvedDate: 2026-07-27`, `sourceRevision: 092642d`) and carries non-runtime rows (`pto: url:null + unsupportedReason`; `urlTemplate` rows; `liveConfirmationRequired` flags) that do not map 1:1 onto the runtime `Record<ClockifyService, string>` map.
- The drift protection is direction-aware: `wrapper/tests/routing-matrix-equality.test.ts:88-119` asserts that a service added to the matrix is visible in the `ClockifyService` union — a naive generated copy would not guard that.

**Strongest reason to leave alone:** single-sourcing breaks the published-package boundary or inverts the approval-evidence direction; the equality test is the deliberate, cheaper bridge.

### H4 — "~40 gates; merge or remove many" → **REFUTED**

Premise false (179 targets; ~6 aggregates). Structure already minimal: the Makefile deliberately keeps no duplicated leaf lists — `node scripts/verify.mjs` is the "sole fast/full command authority" (Makefile:165-168 comment) and `aggregate-gates` checks exact-once execution and ordering (`Makefile:171-179`).

Merging a target reds, simultaneously: `gate-tier-inventory-drift` (inventory regenerates from the Makefile), `contract-inventory`, `docs-counts`, `quality-gates.md` content, `enterprise-hardening-audit` registration, and the **live-evidence currentness record** (Makefile SHA-256 is in `docs/live-evidence-currentness.json` inputHashes). The Makefile text is also consumed by four checkers that assert literal `node --test scripts/<name>.test.mjs` substrings (documented in `docs/test-wiring-contract.json` purpose).

Historical catch record — gates that protect **shipped behavior**: `version-consistency` (releases 1.0.0/1.0.1, commits 54e7075/fa1673c), `official-openapi-drift` (161/168 promotion, 4a49d17), `consumer-cast-budget` (1,463-case proof, risk-register.json:381), `mutation-score` (49462f5, 80f48bb), `sdk-public-api` + `pack-snapshot-check` (release workflows). Gates that protect **process**: agent-handoff, docs-index-drift, gate-tier-inventory-drift. The repo's own post-mortem (11e1ce2) shows the danger class is *un-wired* gates (openapi-lint went stale for a whole re-snapshot "because no aggregate reaches the target") — the fix that landed was wiring it into `product-contracts`, not deletion.

**Strongest reason to leave alone:** the gate graph is itself the protection; removing wiring is the exact failure mode the repo documented and repaired.

### H5 — "consumer-cast-budget is over-engineered; replace with a lint rule or delete" → **REFUTED**

The engine is a TypeScript `Program`/`TypeChecker` dataflow proof, not a pattern match: `scripts/lib/consumer-cast-governance.mjs` (12,951 lines) + 13,124-line adversarial test suite. It was motivated by real escape-hatch request casts in CLI/MCP write paths: git `b1b4258` ("enforce zero request casts"), `900da3a` ("remove unsafe request body escape"), `2abd367` ("type write boundaries"); the remediation receipts in `docs/roadmap-1.0-receipts/task-07-zero-cast.md`; the cast-removal history in `spec/evidence/discrepancies.md:2663-2700`.

It catches what lint cannot: request-object provenance through helpers, spreads, destructuring, `call`/`apply`/`bind`, any-erased receivers, Function trampolines, wrapper chains (scope documented exhaustively in task-07-zero-cast.md). It found real residue: "The source scan found and removed two residual MCP holiday request-object assertions." The contract pins **zero exceptions at 1.0** (`docs/consumer-cast-budget-contract.json` `canonicalZeroBaseline: true`), and the gate is wired into `release-proof` (Makefile:162) and every release workflow. An eslint rule cannot prove generated-type provenance — it would flag `as` casts but not the helper-function bypass.

**Strongest reason to leave alone:** it is the only mechanism proving CLI/MCP write requests are built from generated request types, and it caught real bugs on its own.

### H6 — "cli/ and mcp/ duplicate logic; centralize into wrapper/" → **CONFIRMED-WITH-LIMITS**

Duplication exists, but it is either generated or boundary-intentional:
- **`error-codes.ts` (298 lines, byte-identical in cli/ and mcp/)** — already single-sourced: emitted into all three packages by `scripts/generate-error-docs.mjs:11-16` from `docs/error-codes.json`, so each independently-published tarball carries its own table.
- **`buildRoutingOptions`/`REGIONAL_PREFIXES`/`KNOWN_REGIONS` in `cli/src/client.ts:13-58` vs `mcp/src/client.ts:14-49`** — real hand duplication (~40 lines), but with different UX error strings (`--subdomain requires --region` vs `CLOCKIFY_SUBDOMAIN requires CLOCKIFY_REGION`), different input channels (argv/rc-file vs env block), different client construction (CLI lazy-imports the SDK root and prints an unconfirmed-region stderr notice; MCP has the `setup_required` `MissingCredentialsError` flow, `mcp/src/client.ts:52-78`), and different envelopes (`cli/src/output.ts` tables, `mcp/src/result.ts` typed results, `mcp/src/orchestration/confirmation.ts` token flow).
- Centralizing means a **new frozen 1.x subpath** in the SDK (classification + size-limit ceiling + verify-dual-build names entry) or a new internal workspace package, plus **peer-floor bumps for both consumers** (the repo has done these deliberately: git 97a0ad2). Cost: ~40 lines × 2 saved; cost: expanded frozen surface.

**Strongest reason to leave alone:** the error-codes duplication is already single-sourced through a generator, and the routing builder is UX-boundary code that would couple two independently-releasing packages to save 40 lines.

### H7 — "AGENTS.md/CLAUDE.md bloated; shrink drastically" → **REFUTED**

`docs/agent-handoff-contract.json` pins exact `mustContain` strings in both files (11 for AGENTS.md, 7 for CLAUDE.md); `make agent-handoff` runs in **perfect-fast** (Makefile:147) and governance-contracts. Drastic shrinking without editing the contract reds `perfect-fast`.

The shrink **already happened**: on 2026-08-01 CLAUDE.md's 9 gotcha subsections (357 lines) moved to `docs/gotchas/` (9 files) with gates green (`.remember/today-2026-08-01.done.md:6`). CLAUDE.md is now 250 lines and points to the topic files (CLAUDE.md:199-227). What remains in AGENTS.md is load-bearing operational truth duplicated nowhere else: the mutation-is-GitHub-only rule with the SSRF story (§0), the per-change-scope gate table (§4), the 30-module invariant, the `wrapper/src` wipe-on-sync rule, and the performance-budget flake caveats.

**Strongest reason to leave alone:** the content is contract-pinned and the consolidation the hypothesis asks for was already executed and registered.

### H8 — "mutation/live-evidence machinery exceeds its value" → **REFUTED**

The mutation gate is the **only** signal that caught the SSRF guard regression (AGENTS.md:22-64), and its cost is minimal: GitHub `workflow_dispatch`-only (`make mutation` refuses locally, Makefile mutation target), so it consumes **zero CI minutes**; the maintenance surface is 3 stryker configs + ~10 checker files + 3 contract docs. The floors are monotonic ratchets validated over complete first-parent history (`docs/mutation-score-contract.json` purpose), and the machinery pins retired paths can never reappear (`check-mutation-score.mjs:274` `LEGACY_MODULE_REPLACEMENT`). Live-evidence machinery similarly produced the 161/168 evidence-gated count and the discrepancy ledger that closes real findings (discrepancies.md entries with statuses).

**Strongest reason to leave alone:** removing it deletes the only net that caught a real security-regression-in-tests, for a saving of zero CI time.

### H9 — "release-please is retired; config/manifest are dead" → **REFUTED**

The **workflow** is retired (git c5e7b97 deleted `.github/workflows/release-please.yml`) and its non-return is pinned by `check-ci-contract.mjs` (risk-register.json:205-213, ci-contract.json:249-250, gotchas/release-ci-handoff.md:10-11). But `scripts/check-version-consistency.mjs:145-177` **reads both files**: the manifest must track every package version exactly, and the config must map each package id to its npm name. The gate is in `release-contracts` (Makefile:156) → `contract-gates` → all four release/CI workflows. Its own test suite (`check-version-consistency.test.mjs:129-303`) asserts that mutating either file fails the gate.

**Strongest reason to leave alone:** deletion reds version-consistency in every CI release workflow; the files are the frozen gate record that survived the workflow retirement.

### H10 — "27 subpath exports + ~30 modules; shrink the surface" → **REFUTED** (at 1.x)

`wrapper/package.json` has **28 exports entries** (root + 27 subpaths). The keep-all decision is recorded with measured evidence: `docs/one-point-zero-classification.json` notes + git 638f9de — 13 single-symbol subpaths each have ≥1 importer in examples/tests/cli/mcp/README; `wrapper/.size-limit.json` holds per-subpath byte ceilings (create-client 4 kB, composed-fetch 7.6 kB, root ESM 2 kB); the observability trio (health 1.2 kB, otel-hooks 4.0 kB, diagnostics 8.2 kB) would grow a health-only import to 13.4 kB if folded, and **CJS consumers cannot tree-shake**. The surface is pinned by `docs/sdk-public-api.json` + `check-sdk-public-api.mjs` + `wrapper/scripts/verify-dual-build.sh` (93-name curated surface + `GENERATED_CORE` leak list) + tsconfig self-reference aliases. At 1.x, removing any subpath is a semver-breaking change; the documented removal path is the `deprecated` classification, and none is unused.

**Strongest reason to leave alone:** 1.0 freezes every import path, and each subpath has a consumer and a measured byte budget.

### H11 — "three-package build should be unified" → **CONFIRMED-WITH-LIMITS** (status quo wins)

For unification: root `package.json` already orchestrates all three builds/tests; `generate-package-versions.mjs` is the shared pre-build step in every package script.

Against: the wrapper alone needs dual ESM/CJS (`tsc -p tsconfig.esm.json && tsc -p tsconfig.cjs.json && bash scripts/finalize-cjs.sh`, which writes `{"type":"commonjs"}` into `dist/cjs` under a `"type":"module"` root); `verify-dual-build.sh` smokes both module systems against the pinned 93-name surface; the wrapper's 5 tsconfigs serve distinct gates (EOPT+noImplicitOverride consumer types; types-bundler/types-public-package breaking-change proofs); **mcp deliberately ships ESM-only** (exports conditions have no `require` branch) and cli ships a single bin entry — a unified build would force CJS onto packages that chose not to ship it, or add conditional machinery. The three packages publish as three tarballs on three tag families with peerDependency floors; a single build pipeline couples their release timing. The existing root orchestration is the correct seam.

**Strongest reason to leave alone:** the format obligations differ per package (dual vs ESM-only), and unification couples three independent release tracks.

---

## Section B — Additional deliberate structures found (H12)

1. **`routing.ts` hand-copy + equality test** — policy-documented evidence/runtime boundary (see H3).
2. **`error-codes.ts` triplication via generator** — single source `docs/error-codes.json`, three emitted copies for independent tarballs (scripts/generate-error-docs.mjs:11-16).
3. **`.release-please-manifest.json` / `release-please-config.json` retained after workflow retirement** — live version-consistency inputs (see H9).
4. **`"pto"` excluded from `ClockifyService`** — `wrapper/internal/routing.ts:45-46` "pto is intentionally excluded"; matrix row `pto: url:null + unsupportedReason` deliberately does NOT default to the api host (docs/service-routing-matrix.json profiles.global.pto); allowlist removal deferred as ROUTE-002 runtime work (matrix nonGoals).
5. **3 operations deliberately unpromoted from live-success** — discrepancies.md:3260 "3 ops remain deliberately unpromoted for lack of live evidence"; 161/168 is evidence-gated (docs-counts derives it).
6. **Base-URL precedence reorder deliberately not fixed** — discrepancies.md:1993 "**Status:** `not-a-bug; intentional`. Recorded so a future audit does not 'fix' the precedence and regress mock/replay."
7. **Quarantined tests** — `check-aggregate-wiring.test.mjs` and `check-mcp-write-safety.test.mjs` deliberately unwired with dispositions in docs/test-wiring-contract.json (in-place tracked-file rewrites; prescribed rewrite paths included).
8. **Retired-path guards** — `check-enterprise-hardening.retired-receipts.test.mjs` (the retired branch was a "latent false-green") and the `confirm-guard.ts` non-reappearance pin (`check-mutation-score.mjs:274`, floor-70 replacement by `mcp/src/tool-risk.ts`).
9. **`GENERATED_CORE` pinned leak list** — `verify-dual-build.sh` freezes transitive root-barrel leaks "frozen + gated rather than implicit + unchecked."
10. **Makefile as hash-pinned campaign input** — `docs/live-evidence-currentness.json` inputHashes.
11. **`cli/src/client.ts` vs `mcp/src/client.ts` routing builders** — UX-boundary duplication with `acknowledgeUnconfirmedRegion` as the documented "deliberate act" (cli:26-30, mcp:25-29).
12. **`output/scratch-*.mjs` committed probes** — unreferenced by design, kept as evidence history.
13. **`docs/gotchas/` split** — CLAUDE.md's 357-line gotcha section deliberately partitioned into 9 topic files (CLAUDE.md:199-227).

---

## Section C — Hidden-consumer map (looks unreferenced, is not)

| Artifact | Hidden consumer | Where |
|---|---|---|
| `.release-please-manifest.json`, `release-please-config.json` | `check-version-consistency.mjs:145-177` | release-contracts → CI release workflows |
| `Makefile` | `docs/live-evidence-currentness.json` inputHashes (SHA-256) | live-evidence campaign |
| `docs/operation-parity.json` | `mcp/tests/operation-parity.behavioral.test.ts:14` (runtime MCP boot) | vitest |
| `docs/one-point-zero-classification.json` | `generate-one-point-zero-inventory.mjs:11-19,122` + test | inventory drift |
| `docs/service-routing-matrix.json` | `wrapper/tests/routing-matrix-equality.test.ts` + `scripts/service-routing-matrix.mjs` | wrapper tests + product-contracts |
| `docs/error-codes.json` | `generate-error-docs.mjs:11-16` → 3 generated TS files | make error-docs + error-docs-drift |
| AGENTS.md / CLAUDE.md text | `check-agent-handoff.mjs` mustContain strings | make agent-handoff (perfect-fast) |
| Makefile recipe substrings | 4 checkers assert `node --test scripts/<name>.test.mjs` literally | test-wiring purpose text |
| `scripts/plan.mjs` topic modules (10) | `plan.mjs:13-22` registry | operator/agent plans |
| `../GOCLMCP/Makefile` + spec | `Makefile:249-252 goclmcp-drift`; `Makefile:255-271 spec-sync-drift` (byte-faithful SHA-256) | perfect-full |
| quarantine entries in `docs/test-wiring-contract.json` | `check-test-wiring.mjs` allowlist | aggregate-gates |
| `docs/live-differential-receipt.json` | `docs/live-differential-contract.json:30` receiptPath; written by `check-live-differential.mjs:167-169` | make live-differential |
| `wrapper/.size-limit.json` per-subpath ceilings | `performance-budgets` gate + `check-performance-budgets.mjs:6` | perfect-fast |
| `docs/sdk-public-api.json` | `check-sdk-public-api.mjs`, `verify-dual-build.sh`, wrapper tsconfig self-reference aliases | sdk-public-api gate |
| `output/scratch-*.mjs` | **none** — genuinely unreferenced (12 files) | — |

---

## Section D — Centralization-would-hurt list

1. **Single-sourcing the routing matrix into runtime code** — breaks the published tarball boundary (docs/ not shipped) or turns a hand-written module into generated output, and loses the approval provenance. The equality test is the cheaper bridge.
2. **Centralizing cli/mcp `error-codes.ts` into wrapper/** — already single-sourced via the generator; a runtime import would add a frozen 1.x subpath and peer-floor bumps for zero authorship savings.
3. **Merging/removing Makefile gates** — reds aggregate-gates, gate-tier-inventory-drift, contract-inventory, quality-gates.md, docs-counts, and forces a live-evidence campaign re-run (Makefile is hash-pinned). Un-wired gates are the documented failure class (11e1ce2).
4. **Shrinking AGENTS.md/CLAUDE.md** — reds agent-handoff (perfect-fast) via contract-pinned mustContain strings.
5. **Deleting release-please manifest/config** — reds version-consistency in all release workflows.
6. **Unifying the build across packages** — forces CJS onto ESM-only mcp/cli or adds conditional machinery; couples three independent release tracks.
7. **Deleting/consolidating docs JSONs** — breaks docs-index (139 links), docs-counts pointers, and the operation-parity runtime test.
8. **Replacing consumer-cast-budget with lint** — loses type-level provenance proof; lint cannot see through helpers/spreads/destructuring.
9. **Merging single-symbol SDK subpaths** — breaks the 1.0 freeze and per-subpath size budgets; CJS consumers pay the folded bundle in full.

---

## Section E — SAFE vs DANGEROUS simplification

**SAFE (with evidence):**
- Deleting `output/scratch-*.mjs` (12 unreferenced evidence probes) — after a ledger sweep; no risk-register/contract references found.
- Extending existing generators (error registry, one-point-zero inventory) rather than adding new machinery.
- Adding topics to `scripts/plan.mjs` instead of new standalone CLIs (already enforced by AGENTS.md:368-373).
- Wiring standalone gates into aggregates (the openapi-lint precedent, fa1673c/11e1ce2) — the sanctioned repair direction.
- Following the documented deprecation path for SDK symbols at 2.0 (`deprecated` classification exists in one-point-zero-classification.json).

**DANGEROUS:**
- Deleting any script, docs JSON, gate target, or agent-guidance section without regenerating the five-anchor registrations (gate-tier-inventory, contract-inventory, quality-gates.md, docs-index, enterprise-hardening-audit) and re-running the live-evidence campaign for Makefile edits.
- Any wrapper subpath removal at 1.x (breaking change, frozen surface).
- Any runtime import of docs/ JSON (impossible in the published tarball).
- Any replacement of type-level proof gates with lint rules.
- Any build unification that forces a module format onto a package that deliberately does not ship it.

---

## Acceptance report
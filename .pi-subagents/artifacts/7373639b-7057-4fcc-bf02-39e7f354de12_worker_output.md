## Completed

Audited the REPORTS + SHARED-REPORTS group of the Clockify OpenAPI discrepancy ledger with fresh live probes against the sacrificial sandbox (`reports.api.clockify.me/v1`, 2026-08-07). All four assigned ledger entries were re-verified live; all mandated cross-checks (servers, x-clockify-live-status stamps, official-live.json POST code, corrected GET params) were completed; 8 new unledgered differences were recorded. All created shared reports were deleted (204s + 404-after-delete + list sweep: `Leftovers: 0`). API key and workspace id redacted everywhere.

Key verdicts:
1. **`reports.weekly.exact-seven-day-window`** — CONFIRMED again: 7-day windows (both inclusive/exclusive boundary forms) → 200; 6/8/14/31-day → 400 code 501 "Please select date range of exactly 7 days for weekly report". Both specs' "max one month (31 days)" descriptions are wrong; corrected spec still needs the upstream source fix.
2. **`shared-reports.create.success-code-201-vs-200`** — RESOLVED confirmed: live POST → **200** (not 201); official-live.json also carries 200.
3. **`reports.*` money units** — minor units (cents) confirmed on every monetary field; the seed's family-specific keys (`totals_summary`, `weekly_totals_summary`, `group_totals_summary`) were **not observed** on any live response (weekly/detailed/summary use plain `totals`; expense/attendance use a `totals` object).
4. **`single-gets.404-405-read-from-list` (shared-reports part)** — bare GET `/shared-reports/{id}` is 200 and accepts **all 7** official-documented query params (dateRangeStart/dateRangeEnd honored — override the stored range; sortColumn validated per report type; page/pageSize accepted); corrected spec documents only `exportType`. Workspace-prefixed GET by id → 405.

Biggest new finding (UNLEDGERED U1): the corrected spec's bare-GET response schema `SharedReport` (list-item shape) does not match the live report-data envelope `{totals, donutChart, groupTotals, groupOne, filters}` — and the official `TimeEntrySummaryReportDto` is also inexact.

## Files Changed
- `/tmp/clockify-openapi-audit/findings/reports.md` — created (147 lines, full audit report: 4 ledger entries, mandated cross-checks, 8 unledgered differences, cleanup proof, residual risks)
- `/tmp/clockify-openapi-audit/probes-reports/*` — raw probe artifacts (responses + headers) retained for independent review
- Repo `/Users/15x/Downloads/WORKING/addons-me/clockify-ts-sdk`: **untouched** (read-only respected)

## Notes
- The repo already had pre-existing uncommitted modifications (`Makefile`, `docs/test-wiring-contract.json`, `scripts/*.test.mjs`, untracked `.ai-audit/`) before this session; I made none of them and staged nothing.
- One residual risk: expense-report cents and FREE-plan 31-day caps are inferred from probe-lab evidence (sandbox appears to be on a paid tier — 31-day and 90-day detailed probes both returned 200).
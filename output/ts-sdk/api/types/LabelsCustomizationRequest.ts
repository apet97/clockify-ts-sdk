// This file was auto-generated by Fern from our API Definition.

/**
 * Represents a label customization request object.
 */
export interface LabelsCustomizationRequest {
    /** Represents invoice amount label. */
    amount: string;
    /** Represents invoice billFrom label. */
    billFrom: string;
    /** Represents invoice billTo label. */
    billTo: string;
    /** Represents invoice description label. */
    description: string;
    /** Represents invoice discount label. */
    discount: string;
    /** Represents invoice dueDate label. */
    dueDate: string;
    /** Represents invoice issueDate label. */
    issueDate: string;
    /** Represents invoice itemType label. */
    itemType: string;
    /** Represents invoice notes label. */
    notes: string;
    /** Represents invoice paid label. */
    paid: string;
    /** Represents invoice quantity label. */
    quantity: string;
    /** Represents invoice subtotal label. */
    subtotal: string;
    /** Represents invoice tax label. */
    tax: string;
    /** Represents invoice tax2 label. */
    tax2: string;
    /** Represents invoice total label. */
    total: string;
    /** Represents invoice totalAmountDue label. */
    totalAmountDue: string;
    /** Represents invoice unitPrice label. */
    unitPrice: string;
}

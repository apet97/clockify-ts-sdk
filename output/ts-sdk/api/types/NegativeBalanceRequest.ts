// This file was auto-generated by Fern from our API Definition.

/**
 * Negative balance data to use for creating or updating the policy.
 */
export interface NegativeBalanceRequest {
    /** Represents negative balance amount. */
    amount?: number | undefined;
    amountValidForTimeUnit?: boolean | undefined;
    /** Represents negative balance period. */
    period?: NegativeBalanceRequest.Period | undefined;
    /** Indicates whether negative balance should reset at the end of the period. */
    shouldReset?: boolean | undefined;
    /** Represents negative balance time unit. */
    timeUnit?: NegativeBalanceRequest.TimeUnit | undefined;
}

export namespace NegativeBalanceRequest {
    /** Represents negative balance period. */
    export const Period = {
        Month: "MONTH",
        Year: "YEAR",
    } as const;
    export type Period = (typeof Period)[keyof typeof Period];
    /** Represents negative balance time unit. */
    export const TimeUnit = {
        Days: "DAYS",
        Hours: "HOURS",
    } as const;
    export type TimeUnit = (typeof TimeUnit)[keyof typeof TimeUnit];
}

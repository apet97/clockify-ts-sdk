// This file was auto-generated by Fern from our API Definition.

import type * as core from "../../../../../core/index.js";

/**
 * @example
 *     {
 *         file: fs.createReadStream("/path/to/your/file"),
 *         workspaceId: "workspaceId",
 *         expenseId: "expenseId",
 *         amount: 1.1,
 *         categoryId: "categoryId",
 *         changeFields: ["USER"],
 *         date: "2024-01-15T09:30:00Z",
 *         userId: "userId"
 *     }
 */
export interface ExpenseUpdateRequest {
    workspaceId: string;
    expenseId: string;
    /** Represents an expense amount as the double data type. */
    amount: number;
    /** Indicates whether expense is billable or not. */
    billable?: boolean;
    /** Represents a category identifier across the system. */
    categoryId: string;
    /** Represents a list of expense change fields. */
    changeFields: ExpenseUpdateRequest.ChangeFields.Item[];
    /** Provides a valid yyyy-MM-ddThh:mm:ssZ format date. */
    date: string;
    file: core.file.Uploadable;
    /** Represents notes for an expense. */
    notes?: string;
    /** Represents a project identifier across the system. */
    projectId?: string;
    /** Represents a task identifier across the system. */
    taskId?: string;
    /** Represents a user identifier across the system. */
    userId: string;
}

export namespace ExpenseUpdateRequest {
    export type ChangeFields = ChangeFields.Item[];

    export namespace ChangeFields {
        export const Item = {
            User: "USER",
            Date: "DATE",
            Project: "PROJECT",
            Task: "TASK",
            Category: "CATEGORY",
            Notes: "NOTES",
            Amount: "AMOUNT",
            Billable: "BILLABLE",
            File: "FILE",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}

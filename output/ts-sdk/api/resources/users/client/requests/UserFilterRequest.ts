// This file was auto-generated by Fern from our API Definition.

import type * as ClockifyApi from "../../../../index.js";

/**
 * @example
 *     {
 *         workspaceId: "64a687e29ae1f428e7ebe303",
 *         accountStatuses: ["LIMITED", "ACTIVE"],
 *         email: "<EMAIL>",
 *         includeRoles: false,
 *         memberships: "NONE",
 *         name: "John",
 *         page: 1,
 *         pageSize: 50,
 *         projectId: "21a687e29ae1f428e7ebe606",
 *         roles: ["WORKSPACE_ADMIN", "OWNER"],
 *         sortColumn: "ID",
 *         sortOrder: "ASCENDING",
 *         status: "ACTIVE",
 *         userGroups: ["5a0ab5acb07987125438b60f", "72wab5acb07987125438b564"]
 *     }
 */
export interface UserFilterRequest {
    /** Represents a workspace identifier across the system. */
    workspaceId: string;
    /** Filters users with the corresponding account status filter. */
    accountStatuses?: ClockifyApi.AccountStatus[];
    /** Filters users by email substring. */
    email?: string;
    /** If true, each user's detailed manager roles are included. */
    includeRoles?: boolean;
    /** Returns users along with workspaces, groups, or projects they have access to. */
    memberships?: UserFilterRequest.Memberships;
    /** Filters users by name substring. */
    name?: string;
    /** Page number. */
    page?: number;
    /** Page size. */
    pageSize?: number;
    /** If provided, returns users that have access to the project. */
    projectId?: string;
    /** Filters users that have any of the specified roles. */
    roles?: UserFilterRequest.Roles.Item[];
    /** Sorting criteria. */
    sortColumn?: UserFilterRequest.SortColumn;
    /** Sorting mode. */
    sortOrder?: UserFilterRequest.SortOrder;
    /** Filters users with the corresponding status. */
    status?: UserFilterRequest.Status;
    /** Filters users that belong to the specified user group IDs. */
    userGroups?: string[];
}

export namespace UserFilterRequest {
    /** Returns users along with workspaces, groups, or projects they have access to. */
    export const Memberships = {
        All: "ALL",
        None: "NONE",
        Workspace: "WORKSPACE",
        Project: "PROJECT",
        Usergroup: "USERGROUP",
    } as const;
    export type Memberships = (typeof Memberships)[keyof typeof Memberships];
    export type Roles = Roles.Item[];

    export namespace Roles {
        export const Item = {
            WorkspaceAdmin: "WORKSPACE_ADMIN",
            Owner: "OWNER",
            TeamManager: "TEAM_MANAGER",
            ProjectManager: "PROJECT_MANAGER",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    /** Sorting criteria. */
    export const SortColumn = {
        Id: "ID",
        Email: "EMAIL",
        Name: "NAME",
        NameLowercase: "NAME_LOWERCASE",
        Access: "ACCESS",
        Hourlyrate: "HOURLYRATE",
        Costrate: "COSTRATE",
    } as const;
    export type SortColumn = (typeof SortColumn)[keyof typeof SortColumn];
    /** Sorting mode. */
    export const SortOrder = {
        Ascending: "ASCENDING",
        Descending: "DESCENDING",
    } as const;
    export type SortOrder = (typeof SortOrder)[keyof typeof SortOrder];
    /** Filters users with the corresponding status. */
    export const Status = {
        Pending: "PENDING",
        Active: "ACTIVE",
        Declined: "DECLINED",
        Inactive: "INACTIVE",
        All: "ALL",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}

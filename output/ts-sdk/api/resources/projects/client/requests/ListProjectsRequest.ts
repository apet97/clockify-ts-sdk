// This file was auto-generated by Fern from our API Definition.

import type * as ClockifyApi from "../../../../index.js";

/**
 * @example
 *     {
 *         workspaceId: "64a687e29ae1f428e7ebe303",
 *         name: "Software Development",
 *         "expense-date": "2024-12-31"
 *     }
 */
export interface ListProjectsRequest {
    /** Represents a workspace identifier across the system. */
    workspaceId: string;
    /** If provided, returns projects whose name contains the provided string. */
    name?: string;
    /** When true, search by name returns only projects whose name exactly matches the name parameter. */
    "strict-name-search"?: boolean;
    /** If true, returns only archived projects. If omitted, returns both archived and non-archived projects. */
    archived?: boolean;
    /** If true, returns only billable projects. If omitted, returns both billable and non-billable projects. */
    billable?: boolean;
    /** If provided, returns projects that contain clients matching any provided ids. */
    clients?: string | string[];
    /** Controls whether the clients filter includes or excludes matching client ids. */
    "contains-client"?: boolean;
    /** Filters projects based on client status. */
    "client-status"?: ClockifyApi.ListProjectsRequestClientStatus;
    /** If provided, returns projects that contain users matching any provided ids. */
    users?: string | string[];
    /** Controls whether the users filter includes or excludes matching user ids. */
    "contains-user"?: boolean;
    /** Filters projects based on user status. */
    "user-status"?: ClockifyApi.ListProjectsRequestUserStatus;
    /** Filters projects based on whether they are used as a template or not. */
    "is-template"?: boolean;
    /** Sorts the results by the given column/field. */
    "sort-column"?: ClockifyApi.ListProjectsRequestSortColumn;
    /** Sorting mode. */
    "sort-order"?: ClockifyApi.ListProjectsRequestSortOrder;
    /** If true, results contain additional information about the project. */
    hydrated?: boolean;
    /** Page number. */
    page?: number;
    /** Page size. */
    "page-size"?: number;
    /** If provided, returns projects that match the provided access. */
    access?: ClockifyApi.ListProjectsRequestAccess;
    /** Represents the maximum number of expenses to fetch. */
    "expense-limit"?: number;
    /** If provided, returns expenses dated before the provided yyyy-MM-dd date. */
    "expense-date"?: string;
    /** If provided, returns projects that contain groups matching any provided ids. */
    userGroups?: string | string[];
    /** Controls whether the userGroups filter includes or excludes matching group ids. */
    "contains-group"?: boolean;
}

# experiments/ holds throwaway generator outputs for the Phase 0
# spike (see /Users/15x/.claude/plans/read-agents-md-...md or AGENTS.md
# successor). Don't commit the heavy outputs — only the comparison
# notes and this .gitignore survive.

# Ignore everything by default
*

# Allow the comparison notes + this file + per-generator log files
!.gitignore
!comparison.md
!*.log
!README.md

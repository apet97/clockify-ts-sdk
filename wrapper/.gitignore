node_modules/
dist/
src/
.stryker-tmp/
reports/
*.tsbuildinfo
.DS_Store

# Pack-snapshot CI artifact (the baseline `.packsnapshot` IS
# committed; only the per-run `.actual` diff target is transient).
.packsnapshot.actual

# Findings this repository cannot remove, pinned by exact fingerprint
# (commit:path:rule:line) so each one names a single line of a single commit.
# Never add a bare path here — that is what .gitleaks.toml allowlists are for,
# and they are reviewed as security decisions.
#
# be78c1c wrote scripts/gitleaks-config.test.sh with the sandbox Clockify API
# key as a literal fixture, to prove the clockify-credential rule matches it.
# The rule did match — in the test file itself. The next commit generates that
# fixture at runtime instead, so the working tree is clean, but the literal
# stays in this branch's history and history is not rewritten here.
#
# The key involved was already public before that commit: it is present on
# origin/main under .pi-subagents/ (see docs/rejected-findings.md). It must be
# rotated. These two lines change nothing about that.
be78c1c2cd1fa840f604425614c2d2d00f47c586:scripts/gitleaks-config.test.sh:clockify-credential:72
be78c1c2cd1fa840f604425614c2d2d00f47c586:scripts/gitleaks-config.test.sh:generic-api-key:72

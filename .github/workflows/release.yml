name: Release clockify-sdk-ts-115 to npm

# A pushed wrapper-v* tag may publish only after the complete proof and exact
# artifact/integrity checks. Manual repository proof runs in ci.yml; this
# workflow is deliberately tag-only and publish-capable.

on:
  push:
    tags:
      - "wrapper-v*.*.*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  publish:
    name: Prove and release clockify-sdk-ts-115
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set release state path
        run: echo "RELEASE_STATE_FILE=$RUNNER_TEMP/clockify-sdk-release-state.json" >> "$GITHUB_ENV"

      - name: Setup Node.js 22.13.0
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.13.0"
          registry-url: "https://registry.npmjs.org"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Initialize release receipt
        working-directory: .
        env:
          RELEASE_WORKFLOW: ${{ github.workflow }}
        run: |
          PACKAGE_NAME="$(node -p "require('./wrapper/package.json').name")"
          PACKAGE_VERSION="$(node -p "require('./wrapper/package.json').version")"
          {
            echo "PACKAGE_NAME=$PACKAGE_NAME"
            echo "PACKAGE_VERSION=$PACKAGE_VERSION"
          } >> "$GITHUB_ENV"
          node scripts/release-state.mjs init \
            --file "$RELEASE_STATE_FILE" \
            --workflow "$RELEASE_WORKFLOW" \
            --event-name "$GITHUB_EVENT_NAME" \
            --ref-name "$GITHUB_REF_NAME" \
            --source-sha "$GITHUB_SHA" \
            --package-name "$PACKAGE_NAME" \
            --version "$PACKAGE_VERSION"

      - name: Verify release source is on origin/main
        working-directory: .
        run: |
          if [ "$GITHUB_EVENT_NAME" != "push" ] || [ "$GITHUB_REF_TYPE" != "tag" ]; then
            echo "::error::Release workflow requires a pushed tag"
            exit 1
          fi
          if ! git merge-base --is-ancestor "$GITHUB_SHA" refs/remotes/origin/main; then
            echo "::error::Release tag commit $GITHUB_SHA is not reachable from origin/main"
            exit 1
          fi

      - name: Install workspaces (root)
        working-directory: .
        run: npm ci

      - name: Generate and verify the SDK
        working-directory: .
        run: |
          make sdk-codegen
          make sdk-codegen-drift sdk-codegen-test

      # Build before test: wrapper/examples import the published subpaths
      # (clockify-sdk-ts-115/ensure), which resolve only once dist/ exists.
      # A developer machine always has a stale dist, so this ordering bug is
      # visible only in a fresh checkout.
      - name: Sync, type-check, build (ESM + CJS), test, verify dual build
        working-directory: .
        run: |
          npm run type-check -w clockify-sdk-ts-115
          npm run build -w clockify-sdk-ts-115
          npm test -w clockify-sdk-ts-115
          npm run build:smoke -w clockify-sdk-ts-115

      - name: Run release-blocking contract proof
        working-directory: .
        run: make contract-gates

      - name: Run release-blocking compatibility proof
        working-directory: .
        run: make release-proof

      - name: Pack (dry-run smoke before publish)
        working-directory: .
        run: npm pack --dry-run -w clockify-sdk-ts-115

      - name: Verify tag matches package version
        working-directory: .
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#wrapper-v}"
          if [ "$GITHUB_REF_NAME" = "$TAG_VERSION" ] || [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match package.json version $PACKAGE_VERSION"
            exit 1
          fi

      - name: Pack exact artifact
        working-directory: .
        run: |
          PACK_DIR="$RUNNER_TEMP/clockify-sdk-pack"
          PACK_JSON="$PACK_DIR/pack.json"
          mkdir -p "$PACK_DIR"
          npm pack -w clockify-sdk-ts-115 --json --pack-destination "$PACK_DIR" > "$PACK_JSON"
          PACKAGE_FILENAME="$(node -e '
            const { readFileSync } = require("node:fs");
            process.stdout.write(JSON.parse(readFileSync(process.argv[1], "utf8"))[0].filename);
          ' "$PACK_JSON")"
          PACKAGE_TARBALL="$PACK_DIR/$PACKAGE_FILENAME"
          LOCAL_INTEGRITY="$(node -e '
            const { createHash } = require("node:crypto");
            const { readFileSync } = require("node:fs");
            process.stdout.write("sha512-" + createHash("sha512").update(readFileSync(process.argv[1])).digest("base64"));
          ' "$PACKAGE_TARBALL")"
          {
            echo "PACKAGE_TARBALL=$PACKAGE_TARBALL"
            echo "LOCAL_INTEGRITY=$LOCAL_INTEGRITY"
          } >> "$GITHUB_ENV"
          node scripts/release-state.mjs set-artifact \
            --file "$RELEASE_STATE_FILE" \
            --path "$PACKAGE_TARBALL" \
            --integrity "$LOCAL_INTEGRITY"

      - name: Generate required SBOM (SPDX JSON)
        working-directory: .
        run: |
          SBOM_DIR="$(mktemp -d "$RUNNER_TEMP/clockify-sdk-sbom.XXXXXX")"
          SBOM_PATH="$RUNNER_TEMP/sbom-${PACKAGE_VERSION}.spdx.json"
          tar -xzf "$PACKAGE_TARBALL" -C "$SBOM_DIR"
          (
            cd "$SBOM_DIR/package"
            npm install --package-lock-only --omit=dev --ignore-scripts \
              --legacy-peer-deps --no-audit --no-fund --loglevel=error
            npm sbom --package-lock-only --omit=dev --sbom-format spdx \
              --sbom-type library --loglevel=error > "$SBOM_PATH"
          )
          test -s "$SBOM_PATH"
          node - "$SBOM_PATH" "$PACKAGE_NAME" "$PACKAGE_VERSION" <<'NODE'
          const { readFileSync } = require("node:fs");
          const [path, expectedName, expectedVersion] = process.argv.slice(2);
          const sbom = JSON.parse(readFileSync(path, "utf8"));
          const described = new Set(sbom.documentDescribes ?? []);
          const roots = (sbom.packages ?? []).filter((pkg) => described.has(pkg.SPDXID));
          if (
            roots.length !== 1 ||
            roots[0].name !== expectedName ||
            roots[0].versionInfo !== expectedVersion
          ) {
            throw new Error("SBOM does not describe the exact package name and version");
          }
          NODE

      - name: Publish to npm (with provenance from publishConfig)
        if: github.event_name == 'push' && github.ref_type == 'tag'
        working-directory: .
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          node scripts/release-publish.mjs \
            --file "$RELEASE_STATE_FILE" \
            --package-name "$PACKAGE_NAME" \
            --version "$PACKAGE_VERSION" \
            --tarball "$PACKAGE_TARBALL" \
            --local-integrity "$LOCAL_INTEGRITY"

      - name: Post-publish smoke install (shared registry harness)
        working-directory: .
        run: |
          if node scripts/registry-smoke.mjs sdk --version "$PACKAGE_VERSION" --timeout-ms 120000; then
            node scripts/release-state.mjs registry-smoke --file "$RELEASE_STATE_FILE" --status passed
          else
            node scripts/release-state.mjs registry-smoke --file "$RELEASE_STATE_FILE" --status failed
            exit 1
          fi

      - name: Check npm provenance/attestation
        working-directory: .
        run: |
          node scripts/release-attestation.mjs \
            --file "$RELEASE_STATE_FILE" \
            --package-name "$PACKAGE_NAME" \
            --version "$PACKAGE_VERSION"

      - name: Create or update GitHub release (+ attach required SBOM)
        if: github.event_name == 'push' && github.ref_type == 'tag'
        working-directory: .
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          REPOSITORY: ${{ github.repository }}
        run: |
          if gh release view "$GITHUB_REF_NAME" > /dev/null 2>&1; then
            gh release edit "$GITHUB_REF_NAME" --title "$GITHUB_REF_NAME"
          else
            gh release create "$GITHUB_REF_NAME" --verify-tag \
              --title "$GITHUB_REF_NAME" \
              --notes "See [wrapper/CHANGELOG.md](https://github.com/${REPOSITORY}/blob/${GITHUB_REF_NAME}/wrapper/CHANGELOG.md) for the full diff."
          fi
          test -s "$RUNNER_TEMP/sbom-${PACKAGE_VERSION}.spdx.json"
          gh release upload "$GITHUB_REF_NAME" \
            "$RUNNER_TEMP/sbom-${PACKAGE_VERSION}.spdx.json" \
            --clobber
          node scripts/release-state.mjs github-release --file "$RELEASE_STATE_FILE" --status present

      - name: Finalize release receipt
        if: always()
        working-directory: .
        env:
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ "$JOB_STATUS" != "success" ]; then
            node scripts/release-state.mjs fail --file "$RELEASE_STATE_FILE"
          fi
          node scripts/release-state.mjs show --file "$RELEASE_STATE_FILE" > "$RUNNER_TEMP/clockify-sdk-release-receipt.json"
          cat "$RUNNER_TEMP/clockify-sdk-release-receipt.json" >> "$GITHUB_STEP_SUMMARY"
          printf '%s\n' release_receipt_finalized >> "$GITHUB_STEP_SUMMARY"

      - name: Upload release receipt
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sdk-release-receipt
          path: ${{ runner.temp }}/clockify-sdk-release-state.json
          if-no-files-found: error
